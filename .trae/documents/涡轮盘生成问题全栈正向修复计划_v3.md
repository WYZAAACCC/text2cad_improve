# 涡轮盘生成问题全栈正向修复计划 v3

> 本计划是 v2 的延续。v2 中的 A1/A2/A3 及 A4 的第一步（metadata.py）已完成。
> v3 聚焦剩余工作：A4 收尾、Phase B（preflight 校验）、Phase C（Prompt 重设计）。

## 一、总结

基于已批准的修复路线（P0→P3），本计划完成剩余 8 项修复：

| Phase | 编号 | 修复内容 | 目标文件 | 依赖 |
|-------|------|----------|----------|------|
| A (P0) | A4 收尾 | import_gate 增加 geometry_postcheck gate flag | `import_artifact.py` | A1 已完成 |
| B (P1) | B1 | 新增 A007 多 groove Z 区间冲突检测 | `dialect.py` | Phase A |
| B (P1) | B2 | 新增 A009 revolve_profile 最小半径校验 | `dialect.py` | Phase A |
| C (P3) | C1 | NODE_PARAMS_SYSTEM_PROMPT 增加 IR 能力边界章节 | `prompt_builders.py` | Phase B |
| C (P3) | C2 | NODE_PARAMS_SYSTEM_PROMPT 增加 trust_level 说明 | `prompt_builders.py` | Phase B |
| C (P3) | C3 | 修复 anti_examples 注入 prompt | `context_builder.py` + `prompt_builders.py` + `orchestrator.py` | Phase B |
| C (P3) | C4 | 修复 TURBINE_DISC_REFERENCE_EXAMPLE 的 r=1 填充段 | `package.py` | Phase B |

**实施顺序**：A4 → B1/B2 并行 → C1/C2/C3/C4 并行

---

## 二、当前状态分析

### 已完成（v2 阶段）

| 编号 | 文件 | 修改内容 | 验证状态 |
|------|------|----------|----------|
| A1 | `geometry_postcheck.py` L125-137 | `ok` 判定增加 `closed`/`is_valid_solid` 检查，fail-closed | 已应用 |
| A2 | `auto_fixer.py` L87-92 + L235-267 | 增加 `SEVERITY_TO_CATEGORY` 映射 + `_apply_fix` 强制 category 检查 | 已应用 |
| A3 | `metadata_v3.py` L82/L117/L165 + `run.py` L361-369 | `unsupported_capabilities` 字段流转 | 已应用 |
| A4-1 | `metadata.py` L9-16 | `REQUIRED_VALIDATION_STAGES` 增加 `geometry_postcheck` | 已应用 |

### 待完成

- **A4 收尾**：`import_artifact.py` 需 4 处修改，将 `geometry_postcheck_ok` 纳入 import_gate
- **B1/B2**：`dialect.py` 需新增 2 条 preflight 规则
- **C1-C4**：4 项 Prompt 重设计

### 关键发现（Phase 1 探索结果）

1. **BasePackage 模型已有 `anti_examples` 字段**（`models.py` L91），且 axisymmetric package 已定义 7 个 anti-examples（`package.py` L290-359），但：
   - `AuthoringContext` 无 `base_package_anti_examples` 字段
   - `build_authoring_context` 不加载 anti_examples
   - `_build_operation_summary` 不注入 anti_examples
   - `build_level2_authoring_prompt` 不加载 anti_examples

2. **A001 已检查 `r <= 0`**（`dialect.py` L125-128），但不检查极小半径（如 r=1.0 填充段）

3. **A004 不读取 `z_position_mm`**（`dialect.py` L237-247），无法检测多 groove Z 区间冲突

4. **CutAnnularGrooveParams** 有 `side`/`inner_dia_mm`/`outer_dia_mm`/`depth_mm`/`z_position_mm`（可选）字段

5. **ProfileStation** 的 `r_mm` 可以是 `DimExprOrFloat`（可能是 dict），需在 preflight 中处理

---

## 三、提议的修改

### Phase A (P0) — A4 收尾：import_gate gate flag 补全

**文件**：`integrations/engineering_tools/src/seekflow_engineering_tools/generative_cad/pipeline/import_artifact.py`

**问题**：`metadata.py` 的 `REQUIRED_VALIDATION_STAGES` 已包含 `geometry_postcheck`，但 `import_artifact.py` 的 gate 体系未同步——`geometry_postcheck_ok` 既不在 `REQUIRED_GATE_FLAGS`、`gate` dict、`REQUIRED_TRUE_FLAGS` 中，也没有实际的检查逻辑。这意味着即使 `geometry_postcheck.ok=false`，import_gate 仍会放行。

**修改 1**（L12-17）：`REQUIRED_GATE_FLAGS` 增加 `"geometry_postcheck_ok"`
```python
REQUIRED_GATE_FLAGS = [
    "step_exists", "metadata_exists", "metadata_valid", "safety_valid",
    "contract_hash_valid", "core_validation_ok", "dialect_semantics_ok",
    "geometry_preflight_ok", "runtime_postconditions_ok", "inspection_ok",
    "geometry_postcheck_ok",
    "native_rebuild_allowed", "step_import_allowed",
]
```

**修改 2**（L37-50）：`gate` dict 增加 `"geometry_postcheck_ok": False`
```python
    gate = {
        "step_exists": False,
        "metadata_exists": False,
        "metadata_valid": False,
        "safety_valid": False,
        "contract_hash_valid": False,
        "core_validation_ok": False,
        "dialect_semantics_ok": False,
        "geometry_preflight_ok": False,
        "runtime_postconditions_ok": False,
        "inspection_ok": False,
        "geometry_postcheck_ok": False,
        "native_rebuild_allowed": False,
        "step_import_allowed": False,
    }
```

**修改 3**（L161 之后，L163 `gate["native_rebuild_allowed"] = False` 之前）：增加 geometry_postcheck 检查逻辑
```python
    # Geometry postcheck — gate flag reflects actual state
    geo_pc = val.get("geometry_postcheck", {})
    gate["geometry_postcheck_ok"] = isinstance(geo_pc, dict) and geo_pc.get("ok") is True
    if not gate["geometry_postcheck_ok"]:
        issues.append({"code": "geometry_postcheck_not_ok", "message": "geometry_postcheck.ok must be true for native import"})
        return {"ok": False, "issues": issues, "metadata": metadata, "gate": gate}
```

**修改 4**（L176-181）：`REQUIRED_TRUE_FLAGS` 增加 `"geometry_postcheck_ok"`
```python
    REQUIRED_TRUE_FLAGS = [
        "step_exists", "metadata_exists", "metadata_valid", "safety_valid",
        "contract_hash_valid", "core_validation_ok", "dialect_semantics_ok",
        "geometry_preflight_ok", "runtime_postconditions_ok", "inspection_ok",
        "geometry_postcheck_ok",
        "step_import_allowed",
    ]
```

**为什么**：`geometry_postcheck` 是 A1 修复后的 fail-closed 关键卡点。如果 import_gate 不强制校验其 `ok` 状态，则 A1 的 fail-closed 承诺被绕过——无效几何仍可导入工程软件。

---

### Phase B (P1) — preflight 校验增强

**文件**：`integrations/engineering_tools/src/seekflow_engineering_tools/generative_cad/dialects/axisymmetric/dialect.py`

#### B1: 新增 A007 多 groove Z 区间冲突检测

**插入位置**：在 A004 循环块结束后（L247 之后）、A005 注释（L249）之前插入。

**问题**：当 LLM 输出多个 `cut_annular_groove` 节点时，如果它们的 Z 区间重叠，后续 groove 会破坏前序 groove 的几何。当前 preflight 无此检测。

**新增代码**：
```python
        # ── A007: 多 groove Z 区间冲突检测 ──
        # 检测多个 cut_annular_groove 的 Z 区间是否重叠。
        # 仅对指定了 z_position_mm 的 groove 进行检测（无 z_position_mm 时
        # 区间依赖 profile zmin/zmax，冲突检测复杂度高且不常见）。
        groove_intervals: list[tuple[float, float, str]] = []  # (zmin, zmax, node_id)
        for n in nodes:
            if n.op != "cut_annular_groove":
                continue
            z_pos = n.typed_params.get("z_position_mm") or n.params.get("z_position_mm")
            depth = n.typed_params.get("depth_mm") or n.params.get("depth_mm", 0)
            side = n.typed_params.get("side") or n.params.get("side", "")
            if not is_finite(z_pos) or not is_finite(depth):
                continue
            # side="front": 从 z_pos 向 +Z 切削 depth → 区间 [z_pos, z_pos+depth]
            # side="rear":  从 z_pos 向 -Z 切削 depth → 区间 [z_pos-depth, z_pos]
            if side == "front":
                zmin, zmax = z_pos, z_pos + depth
            elif side == "rear":
                zmin, zmax = z_pos - depth, z_pos
            else:
                continue
            groove_intervals.append((zmin, zmax, n.id))

        for i in range(len(groove_intervals)):
            for j in range(i + 1, len(groove_intervals)):
                zf1, zr1, nid1 = groove_intervals[i]
                zf2, zr2, nid2 = groove_intervals[j]
                if zf1 < zr2 and zf2 < zr1:  # Z 区间严格重叠
                    issues.append(ValidationIssue(
                        stage=stage, code="a007_groove_z_overlap",
                        message=(
                            f"cut_annular_groove nodes {nid1!r} (Z={zf1:.1f}→{zr1:.1f}) "
                            f"and {nid2!r} (Z={zf2:.1f}→{zr2:.1f}) have overlapping Z intervals. "
                            f"Multiple grooves cutting the same Z region will destroy each other's geometry. "
                            f"Adjust z_position_mm/depth_mm to avoid overlap."
                        ),
                        severity="error", node_id=nid1,
                    ))
```

**为什么**：多 groove Z 区间重叠会导致后序 boolean cut 破坏前序 groove 的几何，产生非闭合实体。这是 LLM 输出多 groove 时的常见错误，preflight 应在几何执行前拦截。

---

#### B2: 新增 A009 revolve_profile 最小半径校验

**插入位置**：在 A001 块结束后（L144 之后）、A002 循环（L147）之前插入。

**问题**：LLM 常输出 `r_mm=1.0` 的"填充段"（如 `hub_lower_fill`/`hub_upper_fill`），这些极小半径段会产生退化几何（细针状实体）。A001 仅检查 `r <= 0`，不拦截极小正值。

**新增代码**：
```python
        # ── A009: revolve_profile 最小半径校验 ──
        # 检测极小半径填充段（0 < r_mm < MIN_PROFILE_RADIUS）。
        # LLM 常输出 r=1.0 的 "hub_lower_fill"/"hub_upper_fill" 段，
        # 这些段产生退化的细针状实体，应移除或增大半径。
        MIN_PROFILE_RADIUS = 0.5  # mm
        for n in nodes:
            if n.op != "revolve_profile":
                continue
            ps = n.typed_params.get("profile_stations") or n.params.get("profile_stations", [])
            for i, s in enumerate(ps):
                r = s.get("r_mm", 0)
                if isinstance(r, dict):
                    continue  # DimExpr, skip — validated at analysis time
                label = s.get("label", "") or ""
                if is_finite(r) and 0 < r < MIN_PROFILE_RADIUS:
                    issues.append(ValidationIssue(
                        stage=stage, code="a009_radius_too_small",
                        message=(
                            f"revolve_profile station {i} (label={label!r}) has radius {r:.3f}mm "
                            f"< minimum {MIN_PROFILE_RADIUS}mm. Sub-millimeter radii produce "
                            f"degenerate needle-like solids. Either increase radius to >= {MIN_PROFILE_RADIUS}mm, "
                            f"or remove this station and let adjacent stations connect directly."
                        ),
                        severity="error", node_id=n.id,
                        expected={"min_r_mm": MIN_PROFILE_RADIUS},
                        actual={"r_mm": r},
                    ))
```

**为什么**：极小半径段（r<0.5mm）在 OCCT 旋转后会产生细针状实体，导致 boolean 操作失败或 B-Rep 拓扑退化。这是涡轮盘 r=1.0 填充段问题的根因，preflight 应在几何执行前拦截。

---

### Phase C (P3) — Prompt 重设计

#### C1: NODE_PARAMS_SYSTEM_PROMPT 增加 IR 能力边界章节

**文件**：`integrations/engineering_tools/src/seekflow_engineering_tools/generative_cad/authoring/prompt_builders.py`

**修改位置**：`NODE_PARAMS_SYSTEM_PROMPT`（L86-144），在末尾（L144 之后）增加 IR 能力边界章节。

**新增内容**：
```
IR CAPABILITY BOUNDARIES (must respect):
- The IR can only represent what the registered dialects support. Do NOT
  attempt to model features outside the selected dialects' capability.
- axisymmetric: ONLY rotationally symmetric solids. Non-axisymmetric
  features (blades, cooling holes, asymmetrical pockets) are NOT
  representable — output closest conservative reference geometry and
  state assumptions.
- revolve_profile describes ONLY the outer contour. Internal features
  (bores, grooves, slots) must use their dedicated cut_* operations.
- cut_rim_slot_pattern produces fir-tree/slot profiles on the OUTER
  circumference only. Internal cooling channels are NOT supported.
- If the user requests unsupported features, output the closest
  representable geometry and add a note in llm_validation_hints.
```

**为什么**：当前 prompt 未明确声明 IR 的能力边界，导致 LLM 尝试用 `revolve_profile` 的 `r_mm` 变化来表达 web/bore 凹陷（产生沙漏形），而非使用 `cut_annular_groove`。明确能力边界可减少此类语义错误。

---

#### C2: NODE_PARAMS_SYSTEM_PROMPT 增加 trust_level 说明

**文件**：同 C1（`prompt_builders.py`）

**修改位置**：`NODE_PARAMS_SYSTEM_PROMPT`，在 C1 新增章节之后追加。

**新增内容**：
```
TRUST LEVEL & SAFETY (mandatory):
- All generative output is reference_geometry or concept_geometry — NEVER
  manufacturing-ready, airworthy, or certified.
- trust_level must be one of: concept_geometry, reference_geometry.
  Do NOT output higher trust levels (e.g. production, certified).
- All 7 safety flags must be explicitly true:
  non_flight_reference_only, not_airworthy, not_certified,
  not_for_manufacturing, not_for_installation, no_structural_validation,
  no_life_prediction.
- The system fills safety/constraints fields — do NOT output them in
  node params. But understand that your geometry is reference-only.
```

**为什么**：当前 prompt 只说"system handles safety"，但未解释 trust_level 的含义和约束。LLM 可能误以为可以输出高精度制造级几何，导致过度设计或参数偏差。明确 trust_level 边界有助于 LLM 输出更保守、更合适的参考几何。

---

#### C3: 修复 anti_examples 注入 prompt

**问题**：`BasePackage.anti_examples` 字段已存在，axisymmetric package 已定义 7 个 anti-examples，但当前 prompt 构建管线完全不加载/注入它们。anti-examples 是引导 LLM 避免常见错误的最有效手段之一。

**涉及 3 个文件**：

##### C3-1: `context_builder.py` — AuthoringContext 增加字段 + 加载逻辑

**修改 1**（L20-32）：`AuthoringContext` 增加 `base_package_anti_examples` 字段
```python
class AuthoringContext(BaseModel):
    """Context for Level-2 authoring — selected dialects only."""

    model_config = ConfigDict(extra="forbid")

    route_plan: RoutePlan
    selected_dialects: list[str]
    dialect_contracts: dict[str, dict]
    level2_usage_skills: dict[str, str]
    base_package_examples: dict[str, list[dict]] = Field(default_factory=dict)
    base_package_anti_examples: dict[str, list[dict]] = Field(default_factory=dict)
    base_package_manifests: dict[str, dict] = Field(default_factory=dict)
    tool_schema_hash: str = ""
    context_hash: str = ""
```

**修改 2**（L58-62）：`build_authoring_context` 增加 `base_package_anti_examples` 局部变量
```python
    selected_dialects: list[str] = []
    dialect_contracts: dict[str, dict] = {}
    level2_usage_skills: dict[str, str] = {}
    base_package_examples: dict[str, list[dict]] = {}
    base_package_anti_examples: dict[str, list[dict]] = {}
    base_package_manifests: dict[str, dict] = {}
```

**修改 3**（L99-106 之后）：在 examples 加载块之后增加 anti_examples 加载
```python
        # 5. Load examples (for few-shot prompt injection)
        examples_list: list[dict] = []
        for ex in pkg.examples:
            if hasattr(ex, "model_dump"):
                examples_list.append(ex.model_dump())
            elif isinstance(ex, dict):
                examples_list.append(ex)
        base_package_examples[did] = examples_list

        # 6. Load anti-examples (for negative guidance prompt injection)
        anti_list: list[dict] = []
        for ae in pkg.anti_examples:
            if isinstance(ae, dict):
                anti_list.append(ae)
        base_package_anti_examples[did] = anti_list
```

**修改 4**（L115-122）：`AuthoringContext` 构造调用增加 `base_package_anti_examples`
```python
    ctx = AuthoringContext(
        route_plan=route_plan,
        selected_dialects=selected_dialects,
        dialect_contracts=dialect_contracts,
        level2_usage_skills=level2_usage_skills,
        base_package_examples=base_package_examples,
        base_package_anti_examples=base_package_anti_examples,
        base_package_manifests=base_package_manifests,
    )
```

**修改 5**（L36-42）：`compute_hashes` 将 anti_examples 纳入 hash 计算
```python
    def compute_hashes(self) -> "AuthoringContext":
        """Compute tool_schema_hash and context_hash."""
        self.context_hash = stable_hash({
            "selected_dialects": sorted(self.selected_dialects),
            "contracts": self.dialect_contracts,
            "usage_skill_hashes": {
                k: stable_hash(v) for k, v in self.level2_usage_skills.items()
            },
            "anti_examples_hashes": {
                k: stable_hash(v) for k, v in self.base_package_anti_examples.items()
            },
        })
        return self
```

##### C3-2: `prompt_builders.py` — `_build_operation_summary` 注入 anti_examples

**修改位置**：`_build_operation_summary`（L337-387），在 examples 注入块（L375-385）之后增加 anti_examples 注入。

```python
    # ── Anti-examples (negative guidance) ──
    if hasattr(ctx, 'base_package_anti_examples') and ctx.base_package_anti_examples:
        parts.append("## Anti-Examples (DO NOT replicate these patterns)")
        for did, anti_list in ctx.base_package_anti_examples.items():
            if not anti_list:
                continue
            parts.append(f"### Dialect: {did}")
            for ae in anti_list:
                title = ae.get("title", ae.get("anti_id", "untitled"))
                explanation = ae.get("explanation", "")
                parts.append(f"#### {title}")
                # Include the bad pattern for clarity
                for k in ("bad_op", "bad_field", "bad_input", "bad_snippet", "bad_profile_stations"):
                    if k in ae:
                        parts.append(f"- Bad {k}: {compact_json(ae[k]) if not isinstance(ae[k], str) else ae[k]}")
                if "correct_approach" in ae:
                    parts.append(f"- Correct approach: {ae['correct_approach']}")
                parts.append(f"- Reason: {explanation}")
                parts.append("")
```

##### C3-3: `orchestrator.py` — `build_level2_authoring_prompt` 加载并注入 anti_examples

**修改位置**：`build_level2_authoring_prompt`（L101-170）

**修改 1**（L134-161 之后的 usage_skills 加载块中）：在加载 usage_skills 时同时加载 anti_examples
```python
    if usage_skills is None:
        usage_skills = {}
        # C3: 同时加载 anti_examples
        anti_examples: dict[str, list[dict]] = {}
        from seekflow_engineering_tools.generative_cad.base_packages.registry import (
            default_base_package_registry,
        )
        from seekflow_engineering_tools.generative_cad.dialects.default_registry import (
            default_registry,
        )
        bp_reg = default_base_package_registry()
        d_reg = default_registry()
        for sd in selection_plan.selected_dialects:
            pkg = bp_reg.get(sd.dialect)
            dialect = d_reg.get(sd.dialect)
            if pkg is not None and dialect is not None:
                usage_skills[sd.dialect] = pkg.level2_usage_markdown
                # C3: 加载 anti_examples
                anti_examples[sd.dialect] = list(pkg.anti_examples) if pkg.anti_examples else []
            elif dialect is not None:
                if strict_usage_skill:
                    usage_skills[sd.dialect] = _build_minimal_usage_skill(dialect)
            elif strict_usage_skill:
                raise ValueError(...)
```

**修改 2**（L163-170）：返回 dict 增加 `anti_examples` 字段
```python
    return {
        "system": LEVEL2_AUTHORING_SYSTEM_PROMPT,
        "user": user_request,
        "output_schema": RawGcadDocument.model_json_schema(),
        "selected_dialects": [sd.model_dump() for sd in selection_plan.selected_dialects],
        "contracts": contracts,
        "usage_skills": usage_skills or {},
        "anti_examples": anti_examples,
    }
```

**为什么**：anti-examples 是引导 LLM 避免常见错误的最有效手段。当前 axisymmetric package 已精心编写 7 个 anti-examples（包括 `regional_profile_stations_bad`、`hourglass_profile_bad` 等），但完全不注入 prompt，造成资源浪费。修复后 LLM 将在 prompt 中看到"DO NOT"模式的明确示例。

---

#### C4: 修复 TURBINE_DISC_REFERENCE_EXAMPLE 的 r=1 填充段

**文件**：`integrations/engineering_tools/src/seekflow_engineering_tools/generative_cad/base_packages/axisymmetric/package.py`

**修改位置**：`TURBINE_DISC_REFERENCE_EXAMPLE` 的 `profile_stations`（L206-211）

**问题**：当前示例包含 4 个 station，其中 2 个是 `r_mm=1.0` 的填充段（`hub_lower_fill`/`hub_upper_fill`）。这些极小半径段会被 B2 新增的 A009 规则拦截，且会误导 LLM 学习错误的半径模式。

**当前代码**（L206-211）：
```python
                    "profile_stations": [
                        {"r_mm": 1.0,  "z_front_mm": 0.0, "z_rear_mm": 5.0,  "label": "hub_lower_fill"},
                        {"r_mm": 30.0, "z_front_mm": 5.0, "z_rear_mm": 35.0, "label": "hub_body"},
                        {"r_mm": 100.0,"z_front_mm": 0.0, "z_rear_mm": 40.0, "label": "rim_outer"},
                        {"r_mm": 1.0,  "z_front_mm": 35.0,"z_rear_mm": 40.0, "label": "hub_upper_fill"},
                    ],
```

**修改后**：
```python
                    "profile_stations": [
                        {"r_mm": 30.0, "z_front_mm": 0.0,  "z_rear_mm": 5.0,  "label": "hub_front"},
                        {"r_mm": 100.0,"z_front_mm": 5.0,  "z_rear_mm": 35.0, "label": "rim_outer"},
                        {"r_mm": 30.0, "z_front_mm": 35.0, "z_rear_mm": 40.0, "label": "hub_rear"},
                    ],
```

**为什么**：
1. 移除 `r_mm=1.0` 填充段，避免被 A009 拦截
2. 3 段连续外轮廓（hub_front → rim_outer → hub_rear）正确展示涡轮盘的 H-R-H 模式
3. `cut_center_bore`（diameter=60mm, 即 r=30mm）会切出中心孔，hub_front/hub_rear 的 r=30mm 与 bore 半径一致，但这是 bore cut 的起始材料，不是 profile 内壁
4. 注意：此修改与 A005（bore-vs-profile coplanar 检测）有潜在冲突——hub_front/hub_rear 的 r=30mm 等于 bore_radius=30mm。但由于 hub_front/hub_rear 是外轮廓段（会被 bore cut 切除），不是内壁站点，A005 的 `abs(r - center_bore_radius) < MARGIN` 检测会触发。**需要在 notes 中说明或调整 r 值**。

**决策**：将 hub_front/hub_rear 的 `r_mm` 从 30.0 调整为 32.0（略大于 bore_radius=30mm，留 2mm 余量），避免 A005 误报：
```python
                    "profile_stations": [
                        {"r_mm": 32.0, "z_front_mm": 0.0,  "z_rear_mm": 5.0,  "label": "hub_front"},
                        {"r_mm": 100.0,"z_front_mm": 5.0,  "z_rear_mm": 35.0, "label": "rim_outer"},
                        {"r_mm": 32.0, "z_front_mm": 35.0, "z_rear_mm": 40.0, "label": "hub_rear"},
                    ],
```

同时更新 `notes`（L278-285）增加说明：
```python
    notes=[
        "Hub(r=32) + Rim(r=100) continuous outer contour. Web is the transition between them — NOT a separate groove.",
        "DO NOT use cut_annular_groove to create a web recess — that makes an unrealistic empty cavity.",
        "DO NOT use profile_stations with zero-width or Z-overlapping stations.",
        "DO NOT use r_mm < 0.5mm filler stations (e.g. r=1.0 hub_lower_fill) — they produce deggenerate needle solids.",
        "hub_front/hub_rear r=32mm is slightly larger than bore_radius=30mm to avoid A005 coplanar detection.",
        "fir-tree slot stations MUST alternate wide (lobe) / narrow (neck) half_width to create undercuts: [6,4,5.5,2.5].",
        "half_width must NOT monotonically decrease — alternating wide/narrow creates the fir-tree locking geometry.",
        "slot depths are nondecreasing: [3, 5, 8, 10].",
    ],
```

---

## 四、假设与决策

### 假设

1. **A1/A2/A3/A4-1 已正确应用**：Phase 1 探索确认了 `geometry_postcheck.py`、`auto_fixer.py`、`metadata_v3.py`、`run.py`、`metadata.py` 的修改均已落地。
2. **`geometry_postcheck` 已在 `run.py` 中写入 validation dict**：`run.py` L309-318 已将 `geo_postcheck` 结果写入 `validation["geometry_postcheck"]`，A4 收尾的 `import_artifact.py` 可直接读取。
3. **`BasePackage.anti_examples` 字段已存在且已填充**：Phase 1 确认 `models.py` L91 有字段定义，`package.py` L290-359 有 7 个 anti-examples。
4. **`ProfileStation.r_mm` 可以是 DimExprOrFloat**：A009 需处理 dict 类型（DimExpr）的 r_mm，跳过而非报错。

### 决策

1. **A007 仅检测有 `z_position_mm` 的 groove**：无 `z_position_mm` 的 groove 区间依赖 profile zmin/zmax，检测逻辑复杂且不常见。简化为只检测显式指定 `z_position_mm` 的 groove 之间的冲突。
2. **A009 阈值设为 0.5mm**：低于此值的 r_mm 在 OCCT 旋转后会产生细针状实体。0.5mm 是工程实践中的合理下限。
3. **C4 的 hub_front/hub_rear r=32mm**：略大于 bore_radius=30mm，留 2mm 余量避免 A005 误报。这比直接用 r=30mm 更安全。
4. **C3 的 `compute_hashes` 纳入 anti_examples**：确保 anti_examples 变更时 context_hash 改变，触发 prompt 重新生成。
5. **C3 不修改 `build_level2_authoring_prompt` 的函数签名**：仅增加返回 dict 的字段，避免破坏现有调用方。anti_examples 通过 dict 字段传递，调用方可选择使用或忽略。

---

## 五、验证步骤

### 1. 语法检查（每项修改后）

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -c "from seekflow_engineering_tools.generative_cad.pipeline.import_artifact import validate_generative_step_artifact_for_native_import; print('A4 OK')"
python -c "from seekflow_engineering_tools.generative_cad.dialects.axisymmetric.dialect import AxisymmetricDialect; print('B1/B2 OK')"
python -c "from seekflow_engineering_tools.generative_cad.authoring.prompt_builders import NODE_PARAMS_SYSTEM_PROMPT; print('C1/C2 OK')"
python -c "from seekflow_engineering_tools.generative_cad.authoring.context_builder import build_authoring_context; print('C3-1 OK')"
python -c "from seekflow_engineering_tools.generative_cad.skills.orchestrator import build_level2_authoring_prompt; print('C3-3 OK')"
python -c "from seekflow_engineering_tools.generative_cad.base_packages.axisymmetric.package import TURBINE_DISC_REFERENCE_EXAMPLE; print('C4 OK')"
```

### 2. 回归测试

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -m pytest tests/ -x -q --tb=short 2>&1 | Select-Object -Last 50
```

重点关注：
- `test_import_artifact*.py` — A4 修改
- `test_axisymmetric_preflight*.py` — B1/B2 修改
- `test_authoring_context*.py` — C3 修改
- `test_turbine_disc*.py` — C4 修改

### 3. A009 自检（验证 C4 修复后示例不再被拦截）

```powershell
python -c "
from seekflow_engineering_tools.generative_cad.base_packages.axisymmetric.package import TURBINE_DISC_REFERENCE_EXAMPLE
from seekflow_engineering_tools.generative_cad.dialects.axisymmetric.dialect import AxisymmetricDialect
# 提取 profile_stations
ps = TURBINE_DISC_REFERENCE_EXAMPLE.raw_document['nodes'][0]['params']['profile_stations']
for s in ps:
    r = s['r_mm']
    assert r >= 0.5, f'r_mm={r} < 0.5 would trigger A009'
print('C4 修复后示例通过 A009 校验')
"
```

### 4. A007 自检（构造冲突场景验证拦截）

```powershell
python -c "
# 验证 A007 能检测两个 z_position_mm 重叠的 groove
# (具体测试在回归测试中覆盖)
print('A007 逻辑已就绪 — 见回归测试')
"
```

### 5. C3 anti_examples 注入验证

```powershell
python -c "
from seekflow_engineering_tools.generative_cad.authoring.context_builder import build_authoring_context
from seekflow_engineering_tools.generative_cad.authoring.schemas import RoutePlan, SelectedDialect
from seekflow_engineering_tools.generative_cad.dialects.default_registry import default_registry
from seekflow_engineering_tools.generative_cad.base_packages.registry import default_base_package_registry
rp = RoutePlan(selected_dialects=[SelectedDialect(dialect='axisymmetric', version='0.2.0')], route_decision='generative_cad_ir')
ctx = build_authoring_context(route_plan=rp, dialect_registry=default_registry(), base_package_registry=default_base_package_registry())
assert hasattr(ctx, 'base_package_anti_examples'), 'missing base_package_anti_examples'
assert 'axisymmetric' in ctx.base_package_anti_examples, 'axisymmetric anti_examples not loaded'
assert len(ctx.base_package_anti_examples['axisymmetric']) >= 5, 'expected >=5 anti_examples'
print(f'C3 OK: loaded {len(ctx.base_package_anti_examples[\"axisymmetric\"])} anti-examples')
"
```

---

## 六、文件修改清单

| # | 文件 | 修改类型 | 行数（约） |
|---|------|----------|------------|
| 1 | `pipeline/import_artifact.py` | A4: 4 处修改 | +12 |
| 2 | `dialects/axisymmetric/dialect.py` | B1: 新增 A007 块 | +28 |
| 3 | `dialects/axisymmetric/dialect.py` | B2: 新增 A009 块 | +22 |
| 4 | `authoring/prompt_builders.py` | C1: NODE_PARAMS_SYSTEM_PROMPT 追加 | +12 |
| 5 | `authoring/prompt_builders.py` | C2: NODE_PARAMS_SYSTEM_PROMPT 追加 | +13 |
| 6 | `authoring/prompt_builders.py` | C3-2: _build_operation_summary 追加 | +18 |
| 7 | `authoring/context_builder.py` | C3-1: 5 处修改 | +12 |
| 8 | `skills/orchestrator.py` | C3-3: 2 处修改 | +8 |
| 9 | `base_packages/axisymmetric/package.py` | C4: profile_stations + notes | +8/-4 |

**总计**：9 个文件，约 133 行修改（含删除）。

---

## 七、风险与缓解

1. **A005 与 C4 的潜在冲突**：C4 将 hub_front/hub_rear 设为 r=32mm，bore_radius=30mm，差值 2mm > MARGIN(1mm)，不会触发 A005。已在 C4 决策中处理。
2. **C3 的 `compute_hashes` 变更可能影响缓存**：anti_examples 纳入 hash 后，现有缓存失效。这是预期行为（anti_examples 是新增的 prompt 内容）。
3. **C3-3 的 `build_level2_authoring_prompt` 返回 dict 增加字段**：现有调用方若使用 `**` 解包或严格 schema 校验可能报错。需检查调用方。**缓解**：在实施时 grep 所有调用方确认兼容性。
4. **B2 的 MIN_PROFILE_RADIUS=0.5mm 阈值**：可能误拦合法的小半径凸台。**缓解**：0.5mm 是工程实践下限，且 A009 只报 error 不删除节点，LLM repair 可调整。
