# 涡轮盘修复计划：P0-P3 收尾验证

## 摘要

本次任务是用户请求"从 P0 到 P3，按照修复的重要性与依赖全部修复"的收尾阶段。经过完整的只读探索确认：

- **v3 计划定义的全部 13 项修复（A1-A4 / A4-1 / B1 / B2 / C1 / C2 / C3-1 / C3-2 / C3-3 / C4）已全部落地到源码**
- 9/10 已识别的测试文件已包含 `geometry_postcheck` 字段
- **剩余仅需 3 个测试文件需要补充 `geometry_postcheck` 字段**即可完成 P0-P3 全部修复的验证收尾

本计划聚焦于这 3 个测试文件的收尾修复，并明确列出不应在本次修复中处理的环境问题和预先存在问题。

---

## 当前状态分析

### 已完成的源码修复（13 项，全部已落地）

| Phase | 编号 | 修复内容 | 文件 | 状态 |
|-------|------|----------|------|------|
| A (P0) | A1 | geometry_postcheck ok 判定含 closed/is_valid | `runtime/geometry_postcheck.py` L125-137 | ✅ |
| A (P0) | A2 | auto_fixer SEVERITY_TO_CATEGORY + category 检查 | `authoring/auto_fixer.py` L87-92, L235-267 | ✅ |
| A (P0) | A3 | unsupported_capabilities 流转 | `pipeline/metadata_v3.py` + `pipeline/run.py` | ✅ |
| A (P0) | A4 | geometry_postcheck_ok 纳入 import gate | `pipeline/import_artifact.py` (4 处) | ✅ |
| A (P0) | A4-1 | REQUIRED_VALIDATION_STAGES 含 geometry_postcheck | `pipeline/metadata.py` L9-16 | ✅ |
| B (P1) | B1 | A007 多 groove Z 区间冲突检测 | `dialects/axisymmetric/dialect.py` L277-314 | ✅ |
| B (P1) | B2 | A009 revolve_profile 最小半径校验 | `dialects/axisymmetric/dialect.py` L146-172 | ✅ |
| C (P3) | C1 | IR 能力边界章节 | `authoring/prompt_builders.py` L145-157 | ✅ |
| C (P3) | C2 | trust_level 说明 | `authoring/prompt_builders.py` L159-169 | ✅ |
| C (P3) | C3-1 | AuthoringContext anti_examples 字段+加载 | `authoring/context_builder.py` (5 处) | ✅ |
| C (P3) | C3-2 | _build_operation_summary 注入 anti_examples | `authoring/prompt_builders.py` L413-431 | ✅ |
| C (P3) | C3-3 | build_level2_authoring_prompt 返回 anti_examples | `skills/orchestrator.py` L134, L150, L172 | ✅ |
| C (P3) | C4 | TURBINE_DISC_REFERENCE_EXAMPLE 修复 | `base_packages/axisymmetric/package.py` L206-211, L277-286 | ✅ |

### 测试文件状态

`REQUIRED_VALIDATION_STAGES`（pipeline/metadata.py L9-16）含 6 个 stage，其中 `geometry_postcheck` 为 A4-1 新增。`validate_generative_metadata_v2` (metadata.py L166-169) 遍历该列表，缺失 stage 会添加 `missing_{key}` issue（该检查独立于 `require_validation_ok` 参数）。

因此，**所有构造 mock metadata 并断言 `validate_generative_metadata_v2(meta)["ok"]` 为 True 的测试**都需要在 validation dict 中包含 `geometry_postcheck` 字段。

#### 已正确修复的测试文件（9 个）

- `test_gcad_v07_import_gate_flags.py` ✅
- `test_gcad_v08_import_gate_flags.py` ✅（且 L65 断言 `len(REQUIRED_GATE_FLAGS) == 13` 正确）
- `test_gcad_v09_import_gate_release.py` ✅
- `test_gcad_v10_import_gate_release.py` ✅
- `test_artifact_state_machine.py` ✅
- `test_vnext_e2e_gate.py` ✅
- `test_gcad_v09_builder_initial_metadata.py` ✅
- `test_gcad_v09_metadata_normalization.py` ✅
- `test_gcad_v10_builder_initial_metadata.py` ✅

#### 不需要修改的测试文件（12 个）

这些文件虽然包含 `geometry_preflight` 但不构造期望通过的 mock metadata，或只检查源代码字符串/失败场景/seed 不被修改：

`experiments/test_corpus.py`、`test_gcad_v05_pipeline_hardening.py`、`test_gcad_v05_metadata_hard_validation.py`、`test_gcad_v06_artifact_builder.py`、`test_gcad_v07_report_schema.py`、`test_gcad_v10_metadata_normalization.py`、`test_gcad_v09_run_artifact_metadata_consistency.py`、`test_gcad_v10_run_metadata_consistency.py`、`test_vnext_metadata_v3_behavior.py`、`test_vnext_legacy_import_barrier.py`、`test_no_production_legacy_imports.py`、`test_runner_requires_validation_proof.py`

#### `test_authoring_pipeline.py` 不需要修改

该文件不构造 mock metadata，而是构造 `RoutePlan`/`FeatureSequenceDraft`/`NodeParamsDraft` 通过 `assemble_raw_gcad_document` 生成 raw gcad document，不经过 `REQUIRED_VALIDATION_STAGES` 检查路径。

---

## 提议变更

### 变更 1：`test_dual_route_isolation.py` 补充 geometry_postcheck 字段

**文件**：`e:\auto_detection_process\integrations\engineering_tools\tests\generative_cad\test_dual_route_isolation.py`

**位置**：L97-101 的 validation dict

**当前代码**（L97-101）：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
},
```

**修改后**：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
    "geometry_postcheck": {"ok": True},
},
```

**原因**：L103-104 调用 `validate_generative_metadata_v2(meta)` 并断言 `result["ok"]`。缺失 `geometry_postcheck` 会触发 `missing_geometry_postcheck` issue 导致断言失败。

### 变更 2：`test_gcad_core_v2.py` 补充 geometry_postcheck 字段

**文件**：`e:\auto_detection_process\integrations\engineering_tools\tests\generative_cad\test_gcad_core_v2.py`

**位置**：L268-272 的 validation dict

**当前代码**（L268-272）：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
},
```

**修改后**：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
    "geometry_postcheck": {"ok": True},
},
```

**原因**：L274-275 调用 `validate_generative_metadata_v2(meta)` 并断言 `result["ok"]`。同上。

### 变更 3：`test_gcad_hardening.py` 补充 geometry_postcheck 字段

**文件**：`e:\auto_detection_process\integrations\engineering_tools\tests\generative_cad\test_gcad_hardening.py`

**位置**：L155-159 的 validation dict（`_valid_meta()` 方法内）

**当前代码**（L155-159）：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
},
```

**修改后**：
```python
"validation": {
    "core_validation": {"ok": True}, "dialect_semantics": {"ok": True},
    "geometry_preflight": {"ok": True}, "runtime_postconditions": {"ok": True},
    "inspection_validation": {"ok": True},
    "geometry_postcheck": {"ok": True},
},
```

**原因**：L162-164 的 `test_valid_passes` 调用 `validate_generative_metadata_v2(self._valid_meta())["ok"]`。`_valid_meta()` 是多个测试的共享工厂方法，修复后所有依赖它的测试都会受益。

---

## 假设与决策

### 决策 1：3 个测试文件的修改方式完全一致

均为在 `inspection_validation` 后添加 `"geometry_postcheck": {"ok": True},`。这是最小必要修改，与已修复的 9 个测试文件保持一致。

### 决策 2：不修改环境问题导致的失败

以下测试失败是环境问题（cadquery 未安装），不应在本次修复中处理：

| 文件 | 原因 |
|------|------|
| `test_handler_geometry.py` | 直接 `import cadquery as cq`，无 `pytest.importorskip` 保护 |
| `test_revolve_profile_fix.py` | 直接 `import cadquery as cq`，无 `pytest.importorskip` 保护 |
| `test_generative_builder.py::TestBuilderCadQuery` | 有 `importorskip`，会 skip 而非失败 |
| `text_to_cad_real/` 下的测试 | DeepSeek API 问题（BadRequestError: Thinking mode 不支持 tool_choice） |

### 决策 3：不修改预先存在的失败

`tests/test_no_fail_open_patterns.py::test_no_except_pass_with_importerror` 会命中 `runtime/cadquery_runtime.py` L30-31 的 `except (ImportError, AttributeError): pass` 模式。这是预先存在的失败，本次修复未改动 `cadquery_runtime.py`，不应在本次修复中处理。

### 假设 1：所有源码修复已正确实现

基于 Phase 1 探索的证据（关键代码片段已验证），13 项源码修复均已正确落地。本计划不再重复修改源码，仅做测试收尾。

### 假设 2：C4 修复已应用

`package.py` L206-211 的 profile_stations 已是 3 段 H-R-H 模式（r=32→100→32），L277-286 的 notes 已含 8 条（包括 A009 禁止条目和 A005 说明）。源码状态显示 C4 已完成，尽管 C4 收尾计划文档将其列为"待办"。

---

## 验证步骤

完成 3 个测试文件修改后，按以下顺序验证：

### 验证 1：语法检查

确认 3 个修改的测试文件无语法错误：

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -c "import ast; [ast.parse(open(f, encoding='utf-8').read()) for f in ['tests/generative_cad/test_dual_route_isolation.py', 'tests/generative_cad/test_gcad_core_v2.py', 'tests/generative_cad/test_gcad_hardening.py']]; print('Syntax OK')"
```

### 验证 2：3 个目标测试通过

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -m pytest tests/generative_cad/test_dual_route_isolation.py::TestMetadataSchemasDistinct::test_generative_metadata_is_v2 tests/generative_cad/test_gcad_core_v2.py::test_metadata_v2_validates "tests/generative_cad/test_gcad_hardening.py::TestMetadataV2Mutation::test_valid_passes" -v
```

预期：3 个测试全部通过。

### 验证 3：A4 相关测试无回归

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -m pytest tests/generative_cad/test_gcad_v08_import_gate_flags.py tests/generative_cad/test_gcad_v09_import_gate_release.py tests/generative_cad/test_gcad_v10_import_gate_release.py tests/generative_cad/test_artifact_state_machine.py tests/generative_cad/test_vnext_e2e_gate.py -v
```

预期：全部通过（已修复的 9 个文件不应因本次修改产生回归）。

### 验证 4：完整回归测试对比

```powershell
cd e:\auto_detection_process\integrations\engineering_tools
python -m pytest tests/ -q --tb=no 2>&1 | Select-Object -Last 10
```

预期：failed 数量从 51 降至 48（减少 3 个）。剩余 48 个失败属环境问题（cadquery 未安装 ~25 个）、DeepSeek API 问题（~9 个）、预先存在的问题（~14 个），均与本次修复批次无关。

---

## 完成标准

- 3 个测试文件的 validation dict 已补充 `geometry_postcheck` 字段
- 验证 1-3 全部通过
- 验证 4 的 failed 数量符合预期（48 ± 2，允许少量波动）
- 不引入新的回归

## 不在本次修复范围内

- cadquery 环境安装（环境问题）
- DeepSeek API 适配（已知的 Thinking mode + tool_choice 冲突）
- `cadquery_runtime.py` 的 `except (ImportError, AttributeError): pass` 模式（预先存在，与本次修复无关）
- 任何源码修改（13 项源码修复已全部完成）
