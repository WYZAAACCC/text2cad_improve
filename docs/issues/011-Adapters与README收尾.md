# Issue 11: Adapters + README 收尾 + 全量测试通过

## Triage

> *This was generated by AI during triage.*

- **Category**: `enhancement`
- **State**: `ready-for-agent`
- **Reason**: 三个 adapter 接口简单（仅 schema 导出，不深度集成）。README 内容要点已列出。验收标准含 pytest/ruff 全部通过。唯一注意点：LangChain adapter 需处理依赖缺失的降级逻辑，已在任务中明确。

## 优先级

P2 — 生态适配和文档收尾

## Blocked by

- [Issue 2: @tool 装饰器与 Schema 生成](./002-tool装饰器与Schema生成.md)
- [Issue 10: Eval 评测框架 + CLI](./010-Eval评测框架与CLI.md)

## 背景

为避免项目变成孤岛，需要提供与其他生态（OpenAI SDK、LangChain、PydanticAI）的最小适配器。同时，README 需要在所有功能完成后做最终定稿，明确项目边界和与 Reasonix 的区别。最终确保所有测试通过。

## 任务

### adapters/openai_compatible.py

```python
def to_openai_tools(registry: ToolRegistry, strict: bool = False) -> list[dict]
```

- 导出标准 OpenAI-compatible tools schema
- 本质上与 DeepSeek 格式相同，确保兼容性

### adapters/langchain.py

```python
def export_langchain_tool_schemas(registry: ToolRegistry) -> list[dict]
```

- 最小化 schema 导出
- 如果用户未安装 LangChain，仅给出提示，不报错
- 不强依赖 LangChain

### adapters/pydantic_ai.py

```python
def export_pydantic_ai_tool_schemas(registry: ToolRegistry) -> list[dict]
```

- 最小化 schema 导出
- 不强依赖 PydanticAI

### README 定稿

- 标题：`# SeekFlow`
- 第一屏清楚说明项目定位：不是 Agent 框架，是工具调用可靠性增强层
- 包含与 Reasonix 的区别说明
- 包含 Quick Start 代码示例
- 包含 MCP Example 代码示例
- 包含 MCP 安全提示（中英文）
- 中文定位说明

### 全量测试

- 确保 `pytest` 全部通过
- 确保 `ruff check` 无错误
- 确保 `mypy` 类型检查通过（至少核心模块）
- 补全所有 example 文件

## 验收标准

- [ ] `to_openai_tools(registry)` 返回合法 OpenAI tools 格式
- [ ] `export_langchain_tool_schemas(registry)` 在无 LangChain 时不报错
- [ ] `export_pydantic_ai_tool_schemas(registry)` 可正常导入和调用
- [ ] README 第一屏明确说明项目不是 Agent 框架
- [ ] README 包含与 Reasonix 的区别说明
- [ ] README 包含 Quick Start 代码（可运行）
- [ ] README 包含 MCP 安全提示
- [ ] `pytest` 全部通过
- [ ] `ruff check` 无错误
- [ ] 所有 5 个 example 文件齐全且可运行

## 测试建议

- adapter 测试验证 schema 导出格式正确，不需要测试与 LangChain/PydanticAI 的真实集成
- README 中的 Quick Start 代码可以直接作为 smoke test 运行

## Parent

PRD: [DeepSeek Tool Reliability Kit](../PRD.md)
