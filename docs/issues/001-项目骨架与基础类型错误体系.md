# Issue 1: 项目骨架 + 基础类型与错误体系

## Triage

> *This was generated by AI during triage.*

- **Category**: `enhancement`
- **State**: `ready-for-agent`
- **Reason**: 所有任务（pyproject.toml、目录结构、types.py、errors.py）在 PRD 和工程文档中已完整定义。接口签名、数据字段、依赖版本均已明确。验收标准可机器检查（pip install 成功、import 不报错）。无需额外信息或人工判断。

## 优先级

P0 — 所有后续 Issue 的前置依赖

## Blocked by

无 — 可立即开始

## 背景

项目需要从零搭建一个 Python 3.10+ 包，使用 `pyproject.toml` 管理。在第一版实现任何功能之前，必须先定义所有模块共享的数据类型和错误体系，这些是后续所有 Issue 的公共同契约。

## 任务

### 项目骨架

- 创建 `pyproject.toml`：项目名 `seekflow`，包名 `seekflow`，Python `>=3.10`，依赖 openai / pydantic / typer / rich 等，CLI 入口 `seekflow` 和 `seekflow`
- 创建源码目录结构 `src/seekflow/`，包含所有子包 `__init__.py`
- 创建 `tests/` 目录和 `conftest.py`
- 添加 `README.md`（含项目定位和 Reasonix 区分说明）
- 添加 `LICENSE`（MIT）
- 添加 `.gitignore`（Python 标准）
- 配置 pytest（`[tool.pytest.ini_options]`）
- 配置 ruff（`[tool.ruff]`）

### 基础类型（`types.py`）

```python
class ToolDefinition(BaseModel):
    name: str
    description: str
    parameters: dict[str, Any]
    func: Callable[..., Any] | None = None
    source: str = "local"
    metadata: dict[str, Any] = Field(default_factory=dict)

class ToolCall(BaseModel):
    id: str | None = None
    name: str
    arguments: str | dict
    raw: dict | None = None

class ToolExecutionResult(BaseModel):
    tool_call_id: str | None = None
    name: str
    arguments: dict
    ok: bool
    result: Any | None = None
    error: str | None = None
    elapsed_ms: int | None = None
    repaired: bool = False
    repair_notes: list[str] = []

class ChatResponse(BaseModel):
    content: str | None = None
    reasoning_content: str | None = None
    tool_calls: list[ToolCall] = []
    finish_reason: str | None = None
    usage: dict | None = None
    raw: Any | None = None

class ToolRuntimeResult(BaseModel):
    final: str
    messages: list[dict]
    tool_results: list[ToolExecutionResult] = []
    trace: Any | None = None
    usage: dict | None = None
```

### 错误体系（`errors.py`）

```python
class SeekFlowError(Exception)
class ToolSchemaError(SeekFlowError)
class StrictSchemaError(SeekFlowError)
class ToolNotFoundError(SeekFlowError)
class ToolArgumentError(SeekFlowError)
class ToolExecutionError(SeekFlowError)
class MCPConnectionError(SeekFlowError)
class MCPToolExecutionError(SeekFlowError)
class EvalConfigError(SeekFlowError)
```

## 验收标准

- [ ] `pip install -e .` 在虚拟环境中成功安装包
- [ ] `python -c "import seekflow"` 不报错
- [ ] `python -c "from seekflow.types import ToolDefinition, ToolCall, ToolExecutionResult, ChatResponse, ToolRuntimeResult"` 全部可导入
- [ ] `python -c "from seekflow.errors import SeekFlowError, ToolNotFoundError, MCPConnectionError"` 全部可导入
- [ ] Pydantic 模型可正常实例化和序列化
- [ ] `requires-python = ">=3.10"` 在 pyproject.toml 中生效
- [ ] `seekflow --help` 显示占位 CLI（可先输出 "SeekFlow CLI"）

## 测试建议

- 验证所有 Pydantic 模型可以实例化并 `model_dump()`
- 验证 Pydantic 模型 JSON Schema 生成正确
- 验证所有错误类继承关系正确且可正常抛出和捕获
- 将 `test_types.py` 放入 `tests/`，即使先只做 smoke test

## Parent

PRD: [DeepSeek Tool Reliability Kit](../PRD.md)
