# 工作记录与改进日志

> **用途**：记录 Claude 在本仓库的改进与工作。此文档可无限延长。
> **规则**：每次用户确认完成一件工作后，将工作追加到本文档，打上标签记号。
> **标签约定**：
> - `[已完成]` — 工作完成并通过用户确认
> - `[进行中]` — 工作尚未完成，仍在推进
> - `[待确认]` — 工作已完成但待用户确认
> - `[阻塞]` — 遇到无法绕过的问题
> - `[经验]` — 过程中积累的教训/发现
> - 日期格式：`[2026-08-03]`

---

## 一、架构文档审计与修正

- **标签**：`[已完成] [2026-08-03]`
- **任务**：以 `docs/架构文档与具体规范.md` 为指导，核对 `integrations/engineering_tools` 中 text-to-cad 真实代码，修正文档飘逸。
- **关键发现**：
  - 真实代码 276 个 .py 文件（文档声称 132，严重过期）
  - 新增 6 个文档未覆盖模块：`prompt_system/`、`validation_kernel/`、`repair_kernel/`、`extensions/`、`skills/domain/`、`topology/ocaf/`
  - validation 层 v0.7 重构（validation_kernel 成为权威引擎，pipeline 降为 wrapper）
  - 修复循环迁移到 repair_kernel；`authoring/repair_agent.py` 是死代码
  - 生产服务器 `app/text-to-cad/server/main.py` 走 prompt_system + repair_kernel
  - `schema_version` 真实值 `"g_cad_core_v0.2"`（文档误写 "0.2.0"）
  - tools.py 实际注册 8 个工具（非 9）；full_authoring 调用链修正
- **产物**：
  - 修正后的 `docs/架构文档与具体规范.md`（2065→2353 行，新增 §20 差异清单）
  - `docs/_drift_audit_draft.md`（核对证据草稿）

---

## 二、涡轮盘生成记录分析

- **标签**：`[已完成] [2026-08-03]`
- **任务**：找出 `app/text-to-cad/server/output` 中最新涡轮盘生成记录，分析流程与盘面/榫槽草图生成。
- **关键发现**：
  - 最新成功记录：`output/b572661c219c4952`（HPT_Disk_KT787_JB_210，Jul-17）
  - 涡轮盘生成已从 axisymmetric 方言迁移到 **sketch_profile 方言**（有序 R-Z 轮廓 + OCCT MakeRevol）+ composition（60 个切割器阵列 + boolean_cut）
  - 盘面：`create_2d_sketch(XZ) → add_polyline(12点R-Z) → close → 4×fillet_sketch → revolve_profile(OCCT MakeRevol)`
  - 榫槽：`create_2d_sketch(XY) → add_polyline(26点枞树形) → close → 3×fillet → extrude_profile`
- **产物**：分析结论（无新文件）

---

## 三、提示词硬编码问题确认

- **标签**：`[已完成] [2026-08-03]`
- **任务**：确认涡轮盘/榫槽草图是硬编码还是 LLM 生成。
- **结论**：**完全硬编码在 `skills/prompts.py:LEVEL2_AUTHORING_SYSTEM_PROMPT`**
  - 盘面 12 点轮廓硬编码（L135-137），llm_raw 输出逐点照抄
  - 榫槽 26 点轮廓硬编码（L197-227）含全部参数
  - `docs/提示词系统升级.md` §3 已批评此问题（涡轮盘知识污染全局提示词）
- **经验**：`[经验]` 提示词中硬编码的专业轮廓会污染所有零件请求，应改为领域技能按需注入。

---

## 四、参数化隔离实验（方案 A）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：隔离主程序，将硬编码轮廓改为参数化描述，验证 LLM 生成不同参数轮廓的可行性。
- **做法**：全部代码在 `_param_experiment/`（主程序零修改），复用 DeepSeekToolCaller。
- **关键发现**：
  - 参数化方案**基本可行**：LLM 能根据参数化文字描述推导轮廓（盘面 12 点、榫槽点数随齿数变化）
  - **小数精度问题**：`to_deepseek_strict_schema` 的 number→integer 转换**不影响输出**（实际 strict=False）；真正原因是提示词未强调精度
  - **方案 A**（提示词强调"保留小数精度"）：fillet 参数 0.8→0.8、1.5→1.5 精确回传，坐标带小数 ✓
- **产物**：
  - `_param_experiment/param_prompts.py`、`run_experiment.py`
  - `output/solutionA_round2/`（4 组合 PNG + JSON）
  - `_param_experiment/test_number_schema.py`（证明 number→integer 非限制）
- **经验**：`[经验]` LLM 默认简化取整，提示词明确"保留输入精度"即可让 LLM 精确回传参数。

---

## 五、完整参数化建模（HB5965 体系）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：查找榫槽参数化建模资料，给出完整参数化建模（弥补早期方案齿高相同、水平连接的不足）。
- **资料依据**：
  - `docs/KT787-JB-215 枞树形榫头、榫槽结构设计技术方法研究及程序软件.docx`（北航 2015，基于航空行业标准 **HB5965-2002**）
  - Fir-tree 优化论文（Tfa/Ufa 齿面角、齿距 2-4mm、齿高:圆角≈1.5:1~2:1）
- **完整参数体系**：全局（slot_count/broach_angle/reference_radius/slot_depth/wedge_angle）+ 每齿数组（tooth_height/thickness/pitch/top_flank/under_flank/tip_fillet/root_fillet/neck）+ 底部
- **产物**：
  - `_param_experiment/PARAMETRIC_MODEL.md`
  - `_param_experiment/fir_tree_parametric.py`（确定性生成器，ground truth）
  - `output/parametric_gt/`（S0-S3 对比图 + 新旧对比）
- **修正记录**（三轮）：
  - 镜像错误（中心对称→轴对称）
  - 齿高语义（x跨度→y方向凸出高度）+ 递增方向
  - 连接线（无平台→水平平台→**沿颈部斜线倾斜**）
  - 底部结构（补最后颈部+外扩+槽底平台+根部）
  - 槽底更高更宽、底部外扩角参数化

---

## 六、参数化能力测试

- **标签**：`[已完成] [2026-08-03]`
- **任务**：验证参数化生成器对多种参数组合的健壮性。
- **结果**：13 个组合全部通过（齿数 1-5、齿高递增/递减/大/小、陡/缓齿面、宽/窄颈部、长连接线）
  - 验证项：轴对称、齿根斜线共线、深度合理
- **产物**：`_param_experiment/test_capability.py` + `output/parametric_capability/`（13 张单独图 + 汇总图）

---

## 七、榫槽 LLM 生成 vs 确定性 — Prompt 迭代

- **标签**：`[进行中] [2026-08-03]` → `[待确认]`
- **任务**：把参数化生成器转为 prompt，让 LLM 生成与确定性生成器一致的点，迭代直到高概率完全正确。
- **迭代历史**：V1~V12
  - 最优：V8/V10（1 示例 + dx 预计算 + retry 反馈）→ T04（标准角度）与 T09（小齿高）**3/3 100%**
  - 本质限制：LLM 对 **x 逐齿精确累加**（大 dx 值）随机失败，连参数值（neck）都近似
- **结论**：纯 prompt 在标准角度/常规参数可达 100%；非常规角度（75°/50°）是 LLM 算术精度本质天花板
- **建议方案**：混合方案（LLM 输出每齿 x 起始位置，程序用确定性公式精确生成点）可稳定 100%
- **产物**：
  - `_param_experiment/llm_slot_iteration.py`（支持 --round/--repeat/--retry）
  - `output/llm_slot_rounds/report_v{1..11}.json`
  - `_param_experiment/SLOT_PROMPT_ITERATION.md`（完整迭代总结）

---

## 八、当前待决策事项

- **标签**：`[待确认] [2026-08-03]`
- 榫槽 LLM 迭代方向：用户选择继续纯 prompt（已试 12 轮）还是混合方案（可 100%）——**等待用户决策**

---

## 九、榫槽 prompt 替换兼容性分析

- **标签**：`[已完成] [2026-08-03]`
- **任务**：检查当前参数化生成的榫槽能否直接替换 server 旧 prompt 生成的榫槽。
- **结论**：**不能直接替换**
  - 格式兼容（add_polyline 点数组）✓
  - 结构不兼容：旧 3点/齿 + 水平颈部平台 vs 新 5点/齿 + 斜线齿根
  - **fillet_sketch 裸索引约 50% 错位**（绑定旧 26 点结构）
- **产物**：`_param_experiment/SLOT_REPLACE_ANALYSIS.md`

---

## 十、榫槽 fillet 圆角策略（角色语义驱动）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：在改进后的参数化榫槽（5点/齿）上测试 fillet_sketch，保证圆角始终正确。
- **核心方案**：**construction_role 角色语义**替代裸顶点索引
  - 轮廓点标注角色（mouth/neck/tip_flank_top/tip_platform_end/connector/bottom_flare/bottom_platform/root）
  - fillet 规则按角色匹配：tip_fillet→齿顶两端、neck_fillet→齿根、bottom_fillet→底部
  - **与齿数/点数完全解耦**（齿数变化自动适配）
- **三个保证机制**：
  1. 角色驱动选顶点
  2. **位置匹配**（fillet2D 后 wire 顶点变化，按原始位置找最近顶点）
  3. **半径约束**（≤ 相邻最短边/2，超限自动减半）
- **验证**：2/3/4 齿圆角全部 PASS（26→44、34→58、42→72 边）
- **产物**：
  - `_param_experiment/fillet_strategy.py`（annotate_roles/compute_fillet_targets/full_indices）
  - `_param_experiment/FILLET_STRATEGY.md`
  - `output/fillet_role_demo.png`（3齿圆角演示）
- **集成建议**：`handle_fillet_sketch` 的 `at_vertex_index` 改造为按角色匹配 + 位置匹配 + 半径约束，则任何点数结构都正确。

---

## 十一、榫槽圆角 — LLM 指定"角 + 半径"方案（保证不漏）

- **标签**：`[已完成] [2026-08-03]`
- **需求**：LLM 指定"要圆角的点 + 两条边 + 半径"，程序在草图中圆角，且不漏任何该圆的角。
- **核心机制**（保证不漏）：
  1. 程序生成**必须圆角的角清单**（几何/角色规则）——每个角 = 顶点 + 两条邻边
  2. LLM 为清单中每个角指定 `radius_mm`（设计决策）
  3. 程序验证覆盖（`required ⊆ provided`，漏角反馈补齐）
  4. 按顶点+邻边执行圆角（位置匹配 + 半径约束）
- **LLM 指定格式**：`{role, tooth_index, radius_mm}`——role+tooth 定位"点"，程序解析出顶点+两条邻边
- **覆盖规则**：2齿=9角、3齿=12角、4齿=15角（= 3×齿数+3）
- **验证**：LLM 全覆盖 2/3齿 PASS（9/9、12/12，无遗漏/多余/重复）；圆角执行 26→35、34→46 边成功
- **产物**：
  - `_param_experiment/fillet_corners.py`（角清单/schema/验证/执行）
  - `_param_experiment/fillet_llm_test.py`（LLM 全覆盖测试）
  - `_param_experiment/FILLET_CORNERS.md`
  - `output/fillet_corners_demo.png`
- **经验**：`[经验]` 用 role+tooth 语义定位（非裸坐标/索引）让 LLM 100% 可靠；角清单由程序推导保证不漏，LLM 只填设计半径。

---

## 十二、榫槽圆角碰撞问题定位与解决

- **标签**：`[已完成] [2026-08-03]`
- **任务**：用户反馈"原始轮廓错、很多地方没圆角、连接出错"，定位根因。
- **根因（用户经验确认）**：**圆角半径过大，齿间两个相邻圆角碰撞**
  - 齿根两端（`neck`+`connector`）共享 2mm 连接线，两角半径之和 ≤ 共享边长（2.00）
  - 齿顶两端（`tip_flank_top`+`tip_platform_end`）共享 2mm 平台，同理
  - 半径过大 → `fillet2D` 失败 → 旧实现静默跳过 → 角没圆角
- **解决**：`compute_safe_radius` 碰撞安全半径
  1. 邻边约束：半径 ≤ min(邻边)/2
  2. 相邻碰撞约束：相邻两角半径之和 ≤ 共享边长
  3. 迭代收敛缩小
- **验证**：
  - 请求 tip=1.2/1.5/2.0 均被 clamp 到 ≤1.0
  - 11 个角全部圆角成功（26→37 边，无静默失败）
  - 相邻角检查全部 OK（半径之和 ≤ 共享边）
- **产物**：
  - `_param_experiment/fillet_corners.py`（新增 compute_safe_radius + execute_fillets 碰撞安全）
  - `_param_experiment/FILLET_CORNERS.md`（补充 §六b 碰撞安全）
  - `output/fillet_collision_safe.png`（演示图）
- **经验**：`[经验]` 相邻角的圆角半径之和必须 ≤ 共享边长；`fillet2D` 失败不能静默跳过（应减半重试或 clamp 半径）。

---

## 十三、多参数组合圆角测试

- **标签**：`[已完成] [2026-08-03]`
- **任务**：碰撞安全圆角在不同参数组合下的健壮性验证。
- **结果**：**12 个组合全部 PASS**
  - 齿数 2~5（角数 11/15/19/23 = 4×齿数+3；**勘误**：bottom_platform 于条目十六加入后 → 4×齿数+4 = 12/16/20/24）
  - 齿高大/小/递增、陡/缓齿面、宽颈部、长连接线
  - **大半径请求**（M12：tip=1.5）被 clamp 到 0.90~1.20（无碰撞）
  - 所有组合无碰撞、全部角圆角成功（边数增加 = 角数）
- **产物**：`_param_experiment/test_fillet_multicombos.py` + `output/fillet_multicombos/`（12 张对比图）

---

## 十四、圆角点位置深度检查 + 下侧圆角 bug 修复

- **标签**：`[已完成] [2026-08-03]`
- **任务**：用户担心圆角改变原有点位置；深度检查发现下侧未圆角。
- **Bug 修复**：`list_required_corners` 曾用 key 去重只列出上侧 11 角，下侧镜像角被跳过 → **只圆角上侧**（26→37 而非 48 边）。修复后每角含上下两个物理顶点，圆角两侧（26→48 边）。
- **点位置结论**：
  - **被圆角的尖角顶点（22 个）消失**，被圆弧替换——圆角的本质行为
  - **未圆角的点位置完全保持**（口部、槽底平台端 4 点 KEEP 验证）
  - 圆角仅修剪两条邻边靠近顶点的部分（切点距离 = radius/tan(角/2)），碰撞安全半径保证最小侵入
- **修复后 12 组合全部 PASS**（边数 +2×角数，对称）
- **产物**：`output/fillet_point_compare.png`（圆角前后叠加，红=被圆角 绿=保持）

---

## 十五、圆角"显示为直线"问题定位与修复

- **标签**：`[已完成] [2026-08-03]`
- **任务**：用户反馈"圆角是倒角（直线）不是圆弧"。
- **根因**：**是显示问题，非几何问题**。matplotlib 的 `ax.plot(xs,ys)` 用边的两端点连直线，圆弧被渲染为弦（看起来像倒角）。
- **证据**：OCCT 检查圆角后 48 边 = **26 直线 + 22 真圆弧**（`GeomAbs_Circle`，半径 0.5/0.8）——fillet 生成的是真圆弧。
- **修复**：绘图对圆弧边采样 18 点，真实显示圆弧。
- **产物**：`output/fillet_arc_show.png`（真实圆弧显示图）

---

## 十六、圆角清单与 execute_fillets 修复

- **标签**：`[已完成] [2026-08-03]`
- **任务**：用户反馈 11/14（槽底平台端）和齿根角没圆角。
- **修复 1**：`bottom_platform` 角色加入 `FILLET_ROLES` → 槽底平台端 11/14 现在圆角（角清单 11→12 个）。
- **修复 2（关键 bug）**：`execute_fillets` 曾圆角**所有角**（`compute_safe_radius` 对全部角返回默认半径），不管 LLM 请求哪些。修复为只圆角 LLM 请求的角（`safe` 过滤 `requested`）。
- **证据**：修复前"只圆角 neck"也圆角所有角（50 边）；修复后只圆角请求角（32 边）。
- **产物**：`fillet_corners.py` 更新

---

## 十七、齿根连接深度检查 + connector 圆角几何澄清

- **标签**：`[已完成] [2026-08-03]`
- **任务**：用户怀疑"齿顶角影响齿根点位置导致连接出错"。
- **结论**：
  1. **拓扑连续性 PASS**：50 顶点全连 2 边，轮廓闭合，齿根圆角无断裂
  2. **对照实验证伪"齿顶影响齿根"**：只圆角齿顶时齿根点完全保持；齿根点改变是 neck/connector 圆角本身修剪（0.27mm）
  3. **connector 圆弧 C-D 连接的几何澄清**：每个圆角只修剪"它所在角"的两条边；连接线被两端各修剪一次（neck 切 B、connector 切 C），connector 圆弧 C-D 是圆角定义（角的两条边切点）
- **遗留设计问题**：齿根凹槽 = neck圆弧 + 连接线(B-C 1.38mm) + connector圆弧，两个相邻圆角夹直线——**待用户决定**齿根凹槽形式（单圆角/去连接线/增连接线/保持）
- **产物**：`output/fillet_root_zoom.png`、`output/fillet_root_explained.png`

---

## 十八、圆角代码深度分析修复（修 A-E）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：应要求重新深度阅读圆角相关代码，实证问题并修复（深度分析脚本 `deep_fillet_analysis.py` 佐证）。
- **发现并修复**：
  1. **修 A（死代码）**：`compute_safe_radius` 下侧碰撞检查恒被跳过（`idx_sorted` 按 `vertex_idx` 升序 → `lower_vertex_idx` 恒降序 → `ib-ia!=1` 全跳过，实测 15/15）。因上下对称结果仍正确，但未来上下半径不对称会漏检。**修复**：上下侧各自按对应索引升序排序遍历。
  2. **修 B（文档过期）**：角数公式漂移。实际 `4×齿数+4`（2齿12/3齿16/4齿20，含 connector+bottom_platform），但 FILLET_CORNERS.md 写 `3n+3`、条目十三写 `4n+3`。已更新 FILLET_CORNERS.md 三处 + FILLET_STRATEGY.md 勘误 + 条目十三勘误。
  3. **修 C（角色矛盾）**：`fillet_strategy.compute_fillet_targets` bottom 组缺 connector/bottom_platform，与 `fillet_corners.FILLET_ROLES` 不一致。已统一（新增 `connector_fillet` 参数、bottom 组含 `bottom_platform`），并标注权威实现为 fillet_corners.py。
  4. **修 D（静默跳过）**：`execute_fillets` 的 `except: pass` 静默丢弃失败角，与文档"不能静默跳过"矛盾。**修复**：新增可选 `failures` 参数（默认 None 不破坏调用）——整批失败时降半径重试（r→0.7r→0.5r）→ 仍失败逐角隔离定位 → 写入 `failures[key]`。
  5. **修 E（跨界假边）**：`list_required_corners` 上侧用 `(i±1)%n_upper` 取邻居 → mouth/root 跨界点取到假邻居（root 的 `edge_b` 曾是 `root→mouth` 假边）。当前因 root 真邻边也大、mouth 非角而无实际影响，但几何上错误。**修复**：mouth 前驱=镜像(total-1)、root 后继=root镜像(n_upper)。
- **验证**：`test_fillet_multicombos` 12 组合 PASS（输出与修复前逐项一致，角数 12/16/20/24）；深度分析确认下侧检查不再跳过（15/15 命中）、32 角全被圆弧替换（半径 0.5/0.6/0.8）；**修 D 注入测试**：radius=7.0（0.5r 仍超切点约束）→ `failures={'tip_flank_top@0': ...}` 正确填充，其余角不受影响，正常路径 failures 空；跨界修复后 root `edge_b = [-34.888,2.5]→[-34.888,-2.5]`（真镜像邻边）。
- **观察（未改，保守设计）**：OCC fillet2D 真实约束是**切点距离 ≤ 邻边**（`r/tan(θ/2) ≤ L`），对钝角允许比 `min(邻边)/2` 更大的半径（实测 tip 钝角 r=2.5 合法）。`compute_safe_radius` 用 `min(邻边)/2` 是**过度保守**的 clamp——不改变正确性，仅让圆角偏小；若需更大圆角可改为按切点约束计算。
- **经验**：`[经验]` 上下镜像对称结构下，若按"单侧索引"排序做相邻检查，镜像侧索引恒降序会使其成为死代码；闭合轮廓跨界点（首尾）取邻居不能用 `%n_upper`（上/下侧分界处是真实邻边）；文档角数公式随角色集合演进（加 connector/bottom_platform）需同步更新。

---

## 十九、LLM 自由选择圆角（点 + 两条边）机制

- **标签**：`[已完成] [2026-08-03]`
- **需求**：改变"程序定角清单 + LLM 填半径"（强制全覆盖）→ **LLM 自由选择需要圆角的"点 + 两条边"**，prompt 告诉 LLM 选哪些角。两个确认决策：语义 key 引用（非坐标）+ LLM 指定两条边并校验。
- **设计**（新模块 `fillet_free_select.py`，不改现有"程序定角清单"代码）：
  1. **候选角表** `build_candidate_table`：轮廓全部顶点（含 mouth）的角，每角 = 顶点 key `角色@序号` + 两条邻边 key `from→to`（下侧镜像 key 加 `_m` 后缀，跨界边唯一化如 `root@-1→root@-1_m`）
  2. **schema** `free_select_schema`：`{vertex, edge_a, edge_b, radius_mm}`，vertex/edge 为 string（自由引用，无 enum 限制）
  3. **prompt** `build_selection_prompt`：候选角表逐行列出 + 选角原则（齿根 neck/connector 强烈建议、齿顶 tip 建议、槽底 bottom/root 建议、mouth 通常不圆、半径参考、碰撞注意）+ 硬规则（key 必须逐字抄写）
  4. **解析校验** `resolve_selection`：顶点存在性 / 两条边是否围绕顶点 / 去重；边错**容错自动改用正确邻边** + feedback；非法顶点忽略
  5. **执行**：复用 `execute_fillets`（碰撞安全 clamp + 位置匹配 + failures）——不强制覆盖，LLM 可只选 1 角也可全选
- **验证**：
  - 程序模拟 12 组合全 PASS：候选表 == 必须角清单（排除 mouth）、全选等价现有机制、自由子集（只选齿根）、校验拦截、碰撞安全
  - **真实 DeepSeek**：2齿/3齿 LLM 按 prompt 自由选角，12/12、16/16 全有效，**非法 key=0、边错=0、重复=0**（语义 key 抄写 100% 可靠，含跨界边 `root@-1→root@-1_m` 也抄对）；选角符合工程建议（齿根/齿顶/槽底全选、mouth 不选）；圆角执行成功无 failures
- **产物**：
  - `_param_experiment/fillet_free_select.py`（候选表/schema/prompt/解析校验）
  - `_param_experiment/test_fillet_free_select.py`（程序模拟，12 组合 PASS）
  - `_param_experiment/fillet_free_select_llm.py`（真实 LLM 测试）
  - `_param_experiment/plot_fillet_free_select.py` + `output/fillet_free_select/`（演示图 + 候选表/scene JSON）
- **经验**：`[经验]` LLM 逐字抄写语义 key（`角色@序号`、`from→to`）比裸坐标/索引可靠得多（实测 0 错误）；"自由选择"的正确实现 = 程序给候选表（保证引用合法），LLM 只做"选角 + 填半径"两个决策。

---

## 二十、LLM 全程真实链路测试（生成轮廓 → 自由选圆角 → 执行 → 绘制）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：多参数组合测试，全程模拟真实运行状态：LLM 生成榫槽轮廓点 → 程序验证/修复（回退 GT）→ LLM 自由选择圆角 → 执行 → 绘制最终圆角后榫槽。
- **链路**（`run_llm_free_select_full.py`）：
  1. LLM 生成轮廓点（复用 `llm_slot_iteration` 的 v10/v11 prompt + `call_llm_retry` 精炼）
  2. 若 LLM 点与确定性 GT 不一致（max_dx≥0.1 或点数错）→ **回退 GT**（模拟真实 validation/repair 兜底），标注偏差
  3. 基于最终轮廓建候选角表 → prompt 告诉 LLM 选哪些角
  4. LLM 自由选 `{点+两条边+半径}` → `resolve_selection` 校验容错 → `execute_fillets` 圆角
  5. 绘制：每组合单独大图（左原始右圆角，红点=LLM 选中角）+ 汇总图
- **6 组合**：2/3/4齿、标准/陡(75°)/缓(50°)齿面、大齿高
- **结果**：
  - **6/6 圆角执行成功**（failures 全空，边数增量 = 2×选中角数）
  - **圆角自由选择**：非法 key=0；边抄写错仅 2 处（缓齿面，被 `resolve_selection` 容错自动改用正确邻边 → 圆角仍成功）；重复=0
  - **轮廓生成**：陡/缓齿面 LLM **1 轮精确**；2/3/4齿、大齿高回退 GT（max_dx 0.38~4.95，其中 4齿点数错误）——LLM 逐齿 x 累加精度天花板（已知，见条目七）
  - **LLM 自由体现**：第一轮 4齿 LLM 选了 **21 角（含 mouth 口部）**，第二轮 20 角（不含）——选角随 LLM 判断变化
- **产物**：
  - `_param_experiment/run_llm_free_select_full.py`
  - `output/llm_free_select_full/`：6 单图 + 汇总图 + summary.json（含点坐标可重绘）
- **经验**：`[经验]` 链路健壮性关键——LLM 轮廓点偏差/回退、圆角边抄写错误都不影响最终圆角成功（校验 + 容错修正 + 碰撞安全三保险）；语义 key 选角在真实 LLM 调用中再次 0 非法 key。

---

## 二十一、论文 MCP 质量检查层 — 深度分析与原型验证

- **标签**：`[已完成] [2026-08-03]`
- **任务**：深度阅读 3 份论文文档（0726-V3 正文 / 补充材料 / 0725-V1 第1章及数据集规划），理解"检查生成 CAD 质量的 MCP"，分析其能否实现，并用最新涡轮盘建模数据（`output/b572661c219c4952`，KT787_JB_210）构造 MCP 原型验证。
- **论文 MCP 体系**（DiskCAD-MCP）：统一工具接口接入尺寸测量/特征计数/实体健康/几何裕度/STEP 回读/可视化/质量报告，作为**外层工程质量门**；检查结果转 Repair Ticket 反馈 corrector，区分"程序可执行"与"工程模型正确"。论文量化：漏检率 5.8%→1.0%，多工具完成率 91.7%。
- **可实现性分析**（对照现有代码）：现有 `cadquery_inspect_step`、`ModelInspection`（body/face/edge）、`geometry_postcheck`（closed/valid/volume）已覆盖约 60% 基础。**结论：MCP 可构造**。缺口 = 榫槽专项测量（可迁移 `_param_experiment` 轮廓分析）、尺寸专项测量、参数再生（依赖 Compiler）、可视化、MCP 服务器封装。
- **原型验证**（`mcp_prototype.py`，9 工具 MCP 风格注册）：
  - `check_solid_validity`：STEP 导入，body=1 / faces=3015 / edges=9025 / V=8799358.5mm³ / closed / valid ✅
  - `measure_disc_dimensions`：外径 500 / 中心孔 120 / 轴厚 76 / rim 30 ✅
  - `count_fir_tree_slots`：60 个、节距 26.18mm ✅
  - `measure_fir_tree_slot_profile`：齿数 2 / 槽深 24 / 齿面角 78.7° / 齿根圆角 1.0 ✅
  - `check_slot_pitch_and_ligament`：26.18-14 ≥ 2×6.09 ✅
  - `check_slot_depth_and_rim`：24+11 ≤ 35 ✅
  - `validate_slot_pattern_periodicity` ✅ / `validate_slot_step_roundtrip`（回读体积一致）✅
  - **质量报告：工程验收通过**（5 检查 PASS）
- **关键发现**：① STEP 回读体积与 metadata 中 `geometry_postcheck` 记录完全一致（8799358.512）；② 现有 metadata `inspection_validation = missing_inspection_validation_report`（ok:false）——正是论文 MCP 外层质量门的空缺，原型补上了这一环。
- **产物**：
  - `_param_experiment/mcp_prototype.py`（9 个 MCP 工具原型）
  - `_param_experiment/PAPER_MCP_ANALYSIS.md`（作用分析 + 可实现性对照 + 验证结果）
- **经验**：`[经验]` 论文的 MCP 质量层与现有系统高度重合（inspection/STEP 导入已存在），差距主要在"榫槽专项 + 统一质量门 + MCP 服务器封装"，属增量而非全新架构。

---

## 二十二、MCP 工具集 + MCP 服务器（agentic loop）实施

- **标签**：`[已完成] [2026-08-03]`
- **任务**：实施把握较高的 MCP 工具（参数再生后置），构造 MCP 服务器让 LLM 按"工具说明+schema"发现调用，基准 `output/b572661c219c4952`。
- **MCP 工具集**（`mcp_tools.py`，15 个工具，MCP 风格注册 name/description/schema/handler）：
  - 实体类：`check_solid_validity`（封闭/有效/体积/面/边）、`check_degenerate_geometry`（**新增**，OCP 遍历边/面，`min_edge_length_mm=0.25` 阈值）
  - 测量类：`measure_disc_dimensions`（IR 确定性）、`measure_disc_from_brep`（**新增**，STEP 截面提取中心孔 r=60，与 IR 交叉验证）、`count_fir_tree_slots`、`measure_fir_tree_slot_profile`
  - 约束类：`check_slot_pitch_and_ligament`、`check_slot_depth_and_rim`、`check_adjacent_feature_clearance`（**新增**）
  - 对比类：`compare_slot_profile_to_requirement`（**新增**，5 项容差 0.05mm/0.1°）、`inspect_slot_root_fillet`（**新增**）
  - 交换类：`validate_slot_pattern_periodicity`、`validate_slot_step_roundtrip`
  - 展示类：`render_standard_views`（**新增**，剖视/俯视/榫槽轮廓 PNG）
  - 汇总类：`generate_quality_report`
- **MCP 服务器**（`mcp_server.py`）：多轮 agentic loop —— 全部工具以 OpenAI function schema 注册 + `finish_quality_check` 收尾；循环：LLM 选工具 → 执行 handler → 结果回喂 → 直到 finish/无工具调用/上限。每轮校验工具名防幻觉。
- **验证**：
  - 单元 **16/16 PASS**：体积 8799358.512（与 metadata 一致）、外径 500/499.937（IR vs B-rep 交叉验证）、中心孔截面 r=60、榫槽 60/2齿/节距 26.18、STEP 回读误差 0.0%、无退化边
  - **LLM 端到端**：9 次工具调用全部正确（实体→退化→尺寸→榫槽→约束→回读）、**幻觉工具 0**、`finish_quality_check` 收敛、**验收 True**；LLM 自动产出专业质量报告（含 IR vs B-rep 尺寸偏差 0.063mm、识别"节距/槽深零余量设计"并建议加安全余量）
- **关键 OCP 经验**：`TopExp_Explorer.Current()` 返回 `TopoDS_Shape`，需 `TopoDS.Edge_s()`/`Face_s()` 转换才能用于 `BRepAdaptor_Curve`/`BRepGProp`；STEP 导入须缓存（15MB 首次 28s，缓存后 0.3s）。
- **产物**：`mcp_tools.py` / `mcp_server.py` / `test_mcp_tools.py` + `output/mcp_server/`（llm_qa_report.json + 视图 PNG）
- **经验**：`[经验]` 论文 MCP 的"LLM 按工具说明发现调用"可用现有 `DeepSeekToolCaller` + agentic loop 行为等价实现（finish 工具收尾）；LLM 工具选择 100% 正确、0 幻觉，证明 MCP 服务器可构造且可靠。

---

## 二十三、参数再生 MCP（regenerate_model）

- **标签**：`[已完成] [2026-08-03]`
- **任务**：实施论文 DiskCAD-MCP 的"参数再生"工具（改参数→模型再生→验证仍满足约束），先探索后规划实施。
- **探索结论**（关键）：
  - 生产重建入口 `generative_cad/pipeline/run.py: run_gcad_core_from_files(raw_json, out_step, metadata_path)` 可直接从 raw JSON 执行到 STEP（真实工作，21-49s）
  - **发现主程序既有行为**：`build_generative_cad_model`（builder.py:207-217）覆盖 `metadata["validation"]` 时**丢弃 run 写入的 geometry_postcheck** → `validate_generative_metadata_v3(require_validation_ok=True)` 失败。**不改主程序**，故 regenerate_model 用 `run_gcad_core_from_files`（生产 pipeline 入口，模型真实重建；inspection_validation missing 与生产数据一致）
- **实现**（`mcp_tools.py` 新增，不改主程序）：
  - `PARAM_REGISTRY`：8 个受支持参数（slot_count/slot_distribution_radius/slot_axial_depth/root_fillet/flank_fillet/lobe_top_fillet/disc_hub_web_fillet/disc_web_rim_fillet），各含当前值/合理范围/节点映射
  - `list_regeneratable_params`：LLM 查看可改参数（含当前值/范围）
  - `regenerate_model`：LLM 传 `{param_updates: [{param_key, new_value}]}` → 范围校验 → 应用（对称节点同步改）→ `run_gcad_core_from_files` 重建 → 内置验证（solid/volume/bbox）→ 返回 `new_base_dir`（含 raw_fixed.json+output.step，命名兼容其他 MCP 检查工具）
- **验证**：
  - 单元 **7/7 PASS**：单参数（slot_count 60→48 → 新模型 48 槽/节距 32.725/体积 8922902，实体有效）、多参数（count+root_fillet）、**对称参数**（disc_hub_web_fillet→13，lower/upper 同步=13）、**组合不可行拒绝**（hub_fillet→15 与 web_rim=10 相邻圆角冲突 → 重建失败合理拒绝，即论文不可行设计拒绝）、非法参数越界拒绝
  - **LLM 集成**：5 次调用闭环——check_solid_validity(原始) → **regenerate_model(slot_count→48)** → check_solid_validity(新) → count_fir_tree_slots(**48 槽**) → validate_slot_step_roundtrip → finish 验收 True
  - **主流程无影响**：`git status` 确认 `integrations/engineering_tools/src/` 零改动、原始 `output/b572661c219c4952/` 未触碰
- **产物**：`mcp_tools.py`（+PARAM_REGISTRY/list_regeneratable_params/regenerate_model）、`test_regenerate_model.py`、`output/mcp_server/regen_ws/`（再生模型）
- **经验**：`[经验]` ① `build_generative_cad_model` 有丢 geometry_postcheck 的主程序既有行为，完整 artifact 路径改用 `run_gcad_core_from_files`；② 参数**单独合法但组合不可行**（相邻圆角冲突）会自然触发重建失败 → regenerate_model 合理拒绝，恰实现论文"不可行设计正确拒绝"。

---

*文档创建于 2026-08-03。后续工作完成后追加到此文档。*
