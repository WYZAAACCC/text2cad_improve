# 交接文档：Agent 驱动的涡轮盘结构优化闭环

**读者**：接手的 coding agent。
**日期**：2026-09-19
**仓库**：`E:\text_to_cad_improve\auto_detection_process`

这份文档的目的是让你**不必重读全部会话历史**就能接手。它写的是：东西在哪、
怎么跑、什么不能破坏、**什么已经被证明而什么没有**、以及踩过的坑。

第 4 节（证据边界）和第 5 节（坑）是本文档最有价值的部分——它们是时间和机器
时间换来的，请先读它们再动代码。

---

## 1. 环境：什么在哪，怎么跑

### 1.1 磁盘（重要，与常见假设不同）

| 盘 | 类型 | 文件系统 | 备注 |
|---|---|---|---|
| E: | USB **SSD**（快） | **exFAT** | **无法创建 junction / 符号链接** |
| F: | USB **机械盘**（慢） | NTFS | 空间大 |
| C: | 内置 NVMe | NTFS | **长期紧张，见 5.7** |

**代码在 E，数据在 F。** F 盘布局镜像 E 盘的仓库路径。

exFAT 这一条会否掉一整类方案：任何"数据搬到 F、在 E 留个透明链接保持路径不变"
的思路在这台机器上做不到。当前采用的是**显式回退**，见 1.3。

### 1.2 关键路径

| 用途 | 路径 |
|---|---|
| 仓库根 | `E:\text_to_cad_improve\auto_detection_process\` |
| Python | `E:\text_to_cad_improve\auto_detection_process\.conda\python.exe`（3.11.9） |
| ANSYS 18.1 | `D:\ANSYS181\ANSYS Inc\v181\ansys\bin\winx64\ANSYS181.exe` |
| API key | `E:\text_to_cad_improve\auto_detection_process\_archive\apikey.txt` |
| 结构管线代码 | `integrations\structural\src\seekflow_structural\` |
| 闭环驱动脚本 | `_structural_experiment\run_loop.py` |
| **运行产物（全部）** | `F:\text_to_cad_improve\auto_detection_process\_loop_test\` |
| D01–D32 数据集生成物 | `F:\text_to_cad_improve\auto_detection_process\_param_experiment\output\` |
| **存储索引（先读这个）** | `E:\text_to_cad_improve\auto_detection_process\存储位置索引.md` |

### 1.3 路径解析（改数据位置前必读）

因为 exFAT 不能建链接，数据位置由**两个模块**解析，脚本一律问它们：

| 模块 | 函数 | 解析到 |
|---|---|---|
| `_structural_experiment/_store.py` | `loop_test()` | `_loop_test` 所在盘 → 现在 F |
| `_param_experiment/_paths.py` | `output_root()` / `under(*p)` | `output` 所在盘 → 现在 F |

两者都**同时支持搬迁前后两种布局**，判据是"搬迁后的位置**存在且非空**"。

> **那个"非空"是必须的**：目标目录从创建起就存在，搬完前一直是空的。只判"存在"
> 会把脚本指向一棵空树，表现得像"数据丢了"。这个 bug 我在实现当天就踩到了。

`_param_experiment/*.py` 里原有的 39 处 `_HERE / "output"` 已全部改为
`output_root()`（29 个文件），另有 4 个硬编码绝对路径的也改了。**这些脚本没有
测试覆盖**（需要 API key 和数据集），只做了 `py_compile` 与路径解析验证。

### 1.4 跑测试

```bash
cd /e/text_to_cad_improve/auto_detection_process/integrations/structural
../../.conda/python.exe -m pytest tests -q
# 当前：500 passed
```

**改任何东西之后都要跑它。** 它是这个仓库唯一的自动回归防线。

### 1.5 跑一次闭环

```bash
cd /e/text_to_cad_improve/auto_detection_process

F='F:\text_to_cad_improve\auto_detection_process\_loop_test'
TEMP="$F\tmp" TMP="$F\tmp" ./.conda/python.exe -u _structural_experiment/run_loop.py \
  --revisions 3 \
  --root "$F\run7" \
  --lineage D27 \
  --document "$F\live7\revisions\rev-000001\document.json" \
  --experiment "$F\run3\jobs\jobs\D27-rev-000001\case.json" \
  --feedback-calls 24 --revise-calls 16
```

**`TEMP`/`TMP` 的重定向不是可选的**，原因见 5.7。
**`--experiment` 也不是可选的**，如果你想让结论可归因，见 3.2。

耗时（每轮）：生成 ~9 min + 结构链路 ~18 min（含噪声底研究）。3 轮约 80 min。

---

## 2. 系统在做什么

### 2.1 闭环的形状

`_structural_experiment/run_loop.py` 驱动 `pipeline/iterate.py` 的 `Loop`：

```
rev-N 从上一轮的 document 构建 → 生成几何 → 结构链路 → 求解
  → feedback agent 提改进建议（finding，含可检验的预测）
  → revise agent 把建议**改到 document 上**（不是重新生成）
  → rev-(N+1) 从改过的 document 构建
  → 求解后比对：预测 vs 实测 → 写进知识库
```

### 2.2 结构链路的各阶段

`pipeline/orchestrator.py` 串起来。**实际执行顺序**（取自一次真实运行的
`events.jsonl`，不是文档推测）：

```
preflight → frame → domain → setup → assembly → mesh
          → materialize → solve → postprocess → verify → feedback → complete
```

| 阶段 | 文件 | 做什么 |
|---|---|---|
| preflight | `runtime/caller.py`、`orchestrator.py` | API key、预算、前置检查 |
| frame | `pipeline/frame.py` | 建几何坐标系 |
| domain | `agents/domain.py` | 切哪一块做循环对称扇区（D27 正确值是 **18°**，θ_low 必须落在槽间实心材料） |
| setup | `agents/setup.py` | 材料、温度、转速、叶片离心载荷 |
| assembly | `agents/assembly.py` + `agents/facefind.py` | **选哪些面承载叶片载荷**（真实 TopoDS 面，不是公式）。facefind 做搜索，assembly 落库；传了 `--face-selection` 时 assembly 直接采用、不调 facefind |
| mesh | `agents/mesh.py` | 网格方案 + **两级收敛研究**（噪声底的来源，见 3.3） |
| materialize | `pipeline/materialize.py` | 把选中的面映射到网格节点 |
| solve / postprocess | `pipeline/solve.py` | ANSYS 18.1 求解、读回指标 |
| verify | `agents/verify.py` | 判定数字能否引用 |
| feedback | `agents/feedback.py` | 诊断峰值机制，提改动建议 + **可检验的预测** |
| revise | `agents/revise.py` | 把建议落到 document 上（**由 `Loop` 驱动，不在这条链路里**） |

**每个 agent 的决策都留在 `<job>/agent/<stage>.json`**（含 `trace` 与 `calls`）。
只想看某个决定怎么来的，直接读那个文件。

### 2.3 知识库

`tools/knowledge.py`。一条规则 = 一个 `(mechanism, parameter)` 条目：

- `confirmations` / `refutations` 是**从 observations 计算**的属性，不落盘
- `TRUSTED_AFTER = 3` 次确认且 0 次否决 → `trusted`
- `REFUTED_AFTER = 2` 次否决 → `refuted`（退役，进 `retired_by_mechanism`）
- 否则 `candidate`

**`retired_by_mechanism` 是负面例子的载体**——被否决的规则仍然对 revise agent
可见，作为"这条路走过了、没用"的警告。

### 2.4 决策的形态：document

设计是一份 **document JSON**，不是脚本。反馈改动落在
`/nodes/<feature>/params/...` 路径上，下一轮从改过的那份构建
（`generation.json` 的 `built_from: "document"`）。

---

## 3. 不能破坏的约束

### 3.1 母版隔离

`_param_experiment/turbine_disc_dataset_D01-D32/scripts/` 是**冻结的母版模板**。
每轮运行把模板复制进该轮自己的 workspace，母版**永不写入**。

为什么它是结构地标而不只是数据：**仓库根目录的定位依赖这些目录的存在**，
而且三处探测用的标记**不一致**：

| 位置 | 找什么标记 |
|---|---|
| `tools/parametric.py:find_repo_root()` | `_param_experiment` |
| `core/mesh_feedback.py:_find_repo_root()` | `_structural_experiment` **且** `integrations/engineering_tools/src` |
| `cli.py`（面映射那段） | `_structural_experiment` |

**所以 `_param_experiment` 和 `_structural_experiment` 都不能整个搬走**——
搬走任何一个都会让某条路径找不到仓库根。数据可以搬（`output`、`_loop_test`
已经搬了），目录本身不行。如果将来要统一这个探测逻辑，注意三个调用点要一起改。

**验证方法**：跑完检查 `_param_experiment/.../scripts/*.py` 的 mtime 仍是 8 月。
`Loop` 里还有 `workspace.verify_master_untouched()`（`tools/workspace.py:432`）
每轮自查。

### 3.2 实验条件必须跨轮固定（否则结论无效）

**这是本项目最贵的教训。** 早期 run3 报告"某个改动让峰值降 26%"并写进知识库
——**那个对比是无效的**：两轮用的根本不是同一个载荷工况。

| | rev-1 | rev-2 |
|---|---|---|
| 选面 agent 的意图 | 榫齿**齿面** | **榫槽侧壁** |
| 网格上实际加载面积 | 1,306 mm² | 4,334 mm² |
| 合力向量 | `[+258372, +83950, 0]` | `[−258372, −83950, 0]` ← 反了 |

修法：`solve_bundle()` 新增三个注入口
（`domain` / `mesh_plan` / `face_selection`），`run_loop.py` 把它们跨轮携带，
并支持 `--experiment <某个早先运行的 case.json>` 让**第一轮也受约束**。

**只在轮次内固定是不够的**：不同运行之间也会漂移——run4 第一版让 rev-1 自己
选面，峰值就落到了另一个特征上。

**判据**：问"这一轮里还有什么是被重新决定的？"。凡是每轮重跑 agent 的地方，
就是一个会静默漂移的自由度。

### 3.3 噪声底必须测得出来

`tools/results.py:mesh_noise_floor()` 从 `mesh/convergence/` 的两级网格结果算
"只改网格时数字会动多少"。`score_prediction` 用它判定 `unmoved`。

`agents/mesh.py:_ensure_convergence()` 保证它**在阶段关闭前一定跑过**——
agent 可以提前调用（用敏感度信息指导规划是更好的规划），但阶段会兜底。

**这个兜底是必须的**：它曾经只是"agent 可以调用的工具"，而两条路径都跳过了
它（注入方案那条根本没有 agent；agent 那条 13 次调用后提交、一次都没调，
其中 9 次是同一个重复探查），于是整轮 `floors: {}`，比对退回常量 0.005。

### 3.4 反馈要改产物，不要重新生成

见 5.1。改动必须落在**下一步真正读的那个文件**上。

---

## 4. 证据边界：什么被证明了，什么没有

接手时最危险的事是把推理当成事实。下面是明确分级。

### 4.1 已被真实运行证明

| 结论 | 证据 | 强度 |
|---|---|---|
| 闭环端到端可跑通 | run4（2 轮）、run5（3 轮）、run6 | **强** |
| 实验条件固定后，**管线是确定性的** | run3/run4/run5/run6 的 rev-1 **四次独立运行**峰值**逐位相同**：`peak=1778.258`、`r=208.086524`、`faces=5360`、`area=1305.6044` | **强** |
| 反馈改动确实落到 document 上并被下一轮继承 | `built_from: document` + `document.base.json == 上一轮 document.json` | **强** |
| 知识库能记录否决与确认 | run5：同一条规则 `refuted` + `confirmed` 各一次 → `candidate` | **强** |
| 噪声底确实产出 | run6：`noise_floors` 非空（3.83% / 3.69% / **8.43%** / 0.017%），`limits` 为空 | **强** |
| 回路能在"无可改动"时干净停机 | run6：`applied: []`、`skipped: [F1]`、loop limit 说明原因、**无异常** | **强** |
| 母版未被触碰 | scripts/ mtime 仍是 8 月 | **强** |

### 4.2 只有单元测试证明

| 结论 | 说明 |
|---|---|
| 噪声底的**消费**路径 | `test_iterate.py`：小改动→`unmoved`、大改动→`confirmed`、无研究→报"没有底"。**生产端有真实运行验证，消费端只有单测**（run6 停在 rev-1，没产生第二次比对） |
| `retired_by_mechanism` 真的会亮 | 逻辑有，但**从未有规则达到 2 次否决**，这条路没走过 |
| `trusted`（3 次确认）| **从未走过**。目前连 2 次确认都没有 |

### 4.3 只有推理，没有验证

- `_param_experiment` 那 29 个脚本的改造 —— 只做了 `py_compile` 和路径解析验证，
  它们需要 API key 和数据集才能真跑
- 存储搬迁后 `_store` / `_paths` 的解析 —— 验证过解析结果正确，但没有在这些
  路径下跑过一次完整闭环

### 4.4 已知的物理结论（可以作为先验，但要知道它从哪来）

同一条规则 `stress_concentration::feat_holes_poly.points[4].y_mm`（把轻量化孔
顶点往外推）：

| 改动 | 峰值 | 变化 | 来源 |
|---|---|---|---|
| 6.0（基线） | 1778.258 MPa | — | run4/run5/run6 三次一致 |
| 6.0 → 7.0 | 2011.97 MPa | +13.14% | run4（受控） |
| 6.0 → 7.2 | 2248.098 MPa | +26.42% | run5（受控） |
| 7.2 → 8.0 | 1771.602 MPa | −21.20% | run5（受控） |

**非单调**：7.2 附近有局部最差点，推到 8.0 几乎回到基线。
单看任意两点都会得出相反结论——这正是知识库里那条规则停在 `candidate`
（1 否决 + 1 确认）而不是 `trusted` 或 `refuted` 的原因。

**注意**：run3 曾报"6.0→7.0 降 25.77%"，那是载荷换面造成的假象，不要引用。

---

## 5. 踩过的坑（别重复）

每一条都是真实失败换来的。

### 5.1 反馈改动落在了生成器不读的文件上

**症状**：`get_design_variables` 报出 18 个"自由变量"（`R_mm`、`rim_mm`、
`holes`…），但 revise agent 改完 document 后**没有任何效果**。
**根因**：那些变量名来自一个生成器**不读**的文件。
**教训**：判断"改了没有"的唯一标准是**下一步真正读的那个文件变了没有**，
不是"我写了一个看起来对的文件"。
**现状**：已修。`generation.json` 有 `built_from`，`document.base.json` 与上一轮
`document.json` 可直接对比。

### 5.2 周期探针的读法写反了

**症状**：域 agent 拿到 5 个 order 全是 `0.98888`，于是扫遍因数空间、耗尽预算。
**根因**：探针的判据写反，导致所有候选看起来一样好。
**修法**：回包报告**这几个 order 之间的蕴含关系**（粗周期被细周期蕴含，重复单元
是**最大**的 order）。修后同一个 agent 从 14 次调用耗尽 → 8 次提交 18°。

### 5.3 `profile(axis="theta")` 直接崩

**症状**：agent 自报 `no theta attribute`。
**教训**：工具崩溃比工具返回坏数据更糟——agent 无法从异常里恢复出有用信息。

### 5.4 被证伪的规则对决策者隐形

**症状**：知识库里明明有被否决的规则，revise agent 却看不到，
于是重复同一个失败改动。
**根因**：`get_rules` 的过滤条件把 `refuted` 的条目滤掉了。
**修法**：新增 `retired_by_mechanism` 分组，并在提示词里说明"这是警告不是建议"。

### 5.5 种子规则 id 与回路生成的 id 永不匹配

**症状**：种子规则（8 条）和被证伪的规则统计在**两个不相交的命名空间**里。
**教训**：种子数据的 id 命名必须和运行时生成的用同一个函数产出，否则它们是
两套互不相关的数据。

### 5.6 我自己引入过一个回归（`document_unchanged`）

**症状**：run4 整轮跑死，revise agent 调 `needs_input` 反问
"该不该硬改一个改动？"
**根因**：我加的守卫 `if not workspace.document_edits(): raise` 比它该有的宽——
**一条改动都没声称时也触发**。而"反馈 agent 判定无可改动 → 跳过全部"是完全合法
的结局，此时该守卫让 agent **没有任何合法动作**。
**修法**：`if state.applied and not ...`（只在**声称了**改动时才检查）。
**教训**：守卫的**前提条件**要和它想抓的错误一样精确。加守卫时问："合法但罕见的
路径会不会被它误伤？"

### 5.7 C 盘满会让整条链跑死，而且报错完全指不到原因

**症状**：`SaveAs failed with status PCDM_StoreStatus.PCDM_SS_WriteFailure`，
生成阶段失败，日志里只有一行 "produced no result"。
**根因**：C 盘 0 字节可用 → `tempfile.gettempdir()` 落在 C 盘 →
`topology/ocaf/repository.py:save_temp()` 不带参数时写在那里 → 写失败。
**修法（运行侧）**：把 `TEMP`/`TMP` 指到数据盘（见 1.5）。**这不是可选的。**
**修法（代码侧）**：`pipeline/iterate.py:report_lines()` 现在会把**每一轮自己的
`limits`** 打印出来。以前它只打印 loop 级别的，导致失败原因只存在于
`loop_result.json` 而不在人读的那份报告里。

### 5.8 `Monitor` 的 `tail -f` 到期后进程会滞留，锁住文件

**症状**：robocopy 报"成功"（exit 3）却删不掉源文件，留下半完成状态。
**根因**：监视器"到期"了，但它派出的 `tail` 进程还在，一直开着那个日志文件。
**教训**：搬大目录前先 `Get-Process tail` 清一遍。**exit 0–7 都是 robocopy 的
成功码**，别把 3 当成失败。

### 5.9 PowerShell 删 E 盘路径会被安全守卫拦

**症状**：`Remove-Item on system path 'E:' is blocked`。
**绕法**：改用 bash 的 `rm -rf` / `rmdir`，可以成功。

---

## 6. 当前开放的问题（候选下一步）

按我的判断排序：

1. **`trusted` 路径从未走过**。知识库需要 3 次确认才沉淀规则，目前一条都没有。
   要让一条规则被反复确认，需要 feedback agent **在不同轮次提出同一个方向**——
   而它每轮由 LLM 决定，会漂移。这可能是个真问题（缺"该测什么"的引导），
   也可能只是样本不够。**先跑几轮看它是否自然发生，再决定要不要动。**

2. **噪声底研究有一半是浪费的**。`convergence_check` 的 coarse 层和最终网格
   分辨率**完全相同**（都是 259,592 节点）却是重划重解的。复用能省掉约一半增量
   （现在每轮 +590s）。属优化，不是正确性。

3. **`retired_by_mechanism` 从未点亮**。要触发需要同一条规则被否决两次。

4. **`_param_experiment` 那 29 个脚本无测试覆盖**（见 4.3）。

5. **C 盘长期紧张**。系统盘一直只有几 GB，满过一次就直接让整轮跑死（5.7）。
   如果能让 ANSYS 和 Python 的临时目录彻底离开 C 盘，会稳很多。

---

## 7. 验证协议

改完任何东西，按这个顺序：

1. **跑测试**（1.4）。500 条必须全绿。
2. **改的是路径相关的东西** → 检查解析：
   ```bash
   cd _param_experiment && ../.conda/python.exe -c "import _paths; print(_paths.output_root())"
   cd ../_structural_experiment && ../.conda/python.exe -c "import _store; print(_store.loop_test())"
   ```
3. **改的是链路逻辑** → 跑一次完整闭环（1.5），然后**读这三个文件**：
   - `<root>/loop_result.json` —— 每轮的峰值、观测、判定、噪声底、limits
   - `<root>/knowledge.json` —— 学到了什么
   - `<root>/jobs/jobs/<job>/agent/*.json` —— 各 agent 的决策与 tool trace
4. **改的是 agent 行为** → 先读它的 `tool trace` 找**它在反复问什么问题**
   （连续多次参数几乎相同的调用就是信号），再决定改什么。见第 8 节。
5. **确认母版没被碰**：`_param_experiment/.../scripts/*.py` 的 mtime 应仍是 8 月。

---

## 8. 这个项目的设计哲学（会影响你怎么改）

用户对本系统的定位（原话）：

> "harness 是可供 agent 使用的一系列工具和具体的框架代码……依靠 agent 的能力，
> harness 为 agent 提供足够强力的工具，能完美完成高级仿真工程师级别的自动仿真。"

实践中的含义：**agent 行为不对时，先问"缺哪个测量"，不要先加规则。**
这个项目里 7 次 agent 卡住，7 次真正缺的都是一个测量工具，而两次改措辞都无效。

| agent 症状 | 缺的工具 |
|---|---|
| 反复分页拉 profile | 一次返回整周 |
| 阶数无限翻倍 | 探针自报分辨率 |
| 看到 0.943 不知够不够好 | 报残差**位置** |
| 扫遍 7..72 耗尽预算 | 把无解穷举变成 7 次测量 |
| 5 个 order 全是 0.98888 | 报这几个 order 之间的蕴含关系 |
| 选面 agent 连续 11 次同一条件查询 | 报**整个匹配集的摘要** |

**补集（同样重要）**：如果**回路的正确性依赖某个测量**，光有工具不够，
它必须由**阶段**保证。见 3.3——`convergence_check` 有工具、两条路径都没用它。

**判据**：问"这个量如果不测，回路会不会悄悄降级？"会 → 那是阶段的责任，
不是 agent 的判断题。

---

## 9. 有用的习惯

- **两次测量的结论互相矛盾时，先怀疑对比本身**，不要急着解释物理。
  （run3 的 −26% vs 手工的 +45.65% 就是信号：同一个改动、同一个位置、
  两个相反结论。）
- **回路自己的记录里往往已经有线索**。run3 那次 `side_effects` 明确写着加载面
  应力 +113%，噪声底是空的，峰值位置正好在被推动的顶点上——三条线索都在，
  全被忽略了（包括我）。**先读它自己的警告。**
- 排查失败时先看 `loop_result.json` 的 `revisions[].limits`——**每轮自己的
  limits 里有真正的异常**（5.7 修好之前那里是空的）。

---

## 10. 2026-09-20 接手更新

本轮已完成代码审计并落地第一批 harness 加固，详细拆分和后续顺序见：

`AGENT_HARNESS_UPGRADE_PLAN.md`

Flash 实测与修复链回放汇总见：

`AGENT_FLASH_VALIDATION_2026-09-20.md`

当前验证：

- `integrations/structural`: `556 passed`。
- 所有改动文件的定向 `ruff check` 通过。
- D27 真实数据回放：正确 24 面通过；历史混合 72 面、低半径 8 面被新选面门拒绝。
- Flash 正式 benchmark：D27 25/25、D19 3/3，合计 28/28 精确正确、0 误接受；D27 提交率 100%、中位 6 次调用。
- feedback Flash 回放 run3–run6：4/4 提交，调用数 14–25。
- revise Flash 回放：有 change 的 2 条写出精确 patch；无 change 的 2 条擅自修改被 harness 阻断。

本轮关键改动：

- `facefind` 对 `flank_surface_normal` 增加“外缘半径带 + 强法向族”测量门，拒绝把低半径槽面、非工作齿面或跨族面一起提交。
- `feedback` 提交时强制重读 evidence、要求几何 change 带 prediction、拒绝非几何机制携带几何 change、校验 document 参数真实存在。
- `iterate.score_applied` 修正 relative-only change 的 increase/decrease 方向记录。
- `revise` 增加参数、幅度、完整 accounting 和未授权改动归因门；真实 run3/run4/run5 的 7 次历史 document 写回均通过 magnitude 校验。
- 仿真、feedback、revise 与 CFD expert review 的目标 Agent 默认模型统一为 `deepseek-v4-flash`。

真实配对实验已完成：bore 镜像顶点对 `60 -> 66 mm`，峰值 `1709.359 -> 1688.457 MPa`（-1.2228%），实测 re-mesh 噪声底 3.8119%，按噪声底判定为 `unmoved`，没有误报为设计改进。

本次还修复了成对 meridian 顶点单边修改、ANSYS 切向锚点落到 CPCYC 从节点的问题，并将 frame 归一化贯通 profile、Gmsh、face-to-node mapping、APDL 和 postprocess；revise 现在还会拒绝“数值变了但 case-frame 几何位置没变”的伪修改。

本轮还新增了 document feature dependency graph、最终 STEP design probe、可重放 aggregate evidence，以及论文指标聚合器 `_structural_experiment/probes/paper_metrics.py`。

注意：当前工作树仍包含上一轮 Claude Code 留下的大量未提交改动；不要用 `git checkout --` 或重置工作树来“回到干净基线”。

---

## 11. 2026-09-20 续作交接

本轮完成四条链路，详见 AGENT_HARNESS_UPGRADE_PLAN.md 第 6 节：

1. 非全局 Z 完整贯通：deck_axis_for、case_frame_transform、mesh mapping fallback、canonical mesh normalization、postprocess intent mesh。
2. feature dependency graph v2：mirror、fillet family、pattern、contour、feature bundle；revise 支持多参数联合编辑。
3. 求解前实体效果检查：triangle_cells + target window；D27 bore 回放 target min 59.856 -> 62.819 mm，local area 199259.7 -> 198374.9 mm2，target_changed true。
4. 多族实验：11 族 23 次新 sweep，23/23 exact、0 wrong accepted；与既有 D27/D19 合并 51/51 exact。

当前验证：integrations/structural 581 passed；变更文件定向 ruff 通过。

关键 artifacts：

- multi_selection_summary.json；
- paper_metrics_v4.json；
- design_effect_replay_v4.json；
- feature_graph_metrics.json。

限制：新族 reference 是 deterministic measured oracle；feedback/revise 仍集中在 D27；trusted/refuted 样本不足；非全局 Z 仍缺真实 ANSYS solver-level 对照。


---

## 12. 第二轮续作交接

D19 跨族真实闭环完成三次 solve：bore 60 -> 63 -> 66 mm，峰值 1149.708 -> 1150.936 -> 1152.771 MPa。两次预测下降都被 refuted，load-surface stress side effect 约 +5-6%。证据见 _structural_experiment/output/d19_cross_family_loop_reconstructed.json。

revise 已修复 absolute/relative magnitude 漂移、同参数重复写入和 no-op 写回；干净 D19 rev2 replay 精确写回 mirrored bore pair 66 mm。frame 现在在 setup 前读取参数文件 declared axis，30 度 tilt 的 frame/profile 对称测量已恢复。

限制：rotate_solid 目前只旋转 STEP、不更新 OCAF topology，导致 STEP/XBF frame 不一致；系统会 fail-closed。真实 tilted ANSYS 对照需要 generation topology 支持 rigid transform。

最新指标：paper_metrics_v10.json、paper_tables_v5.md。structural tests: 581 passed。
