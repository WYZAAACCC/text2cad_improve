"""Turbomachinery geometry primitives — reserved for future turbine disk/blade implementations.

Currently empty. Primitive definitions will be added when full implementations
(geometry kernel, compiler handler, metadata, mechanical validator) are ready.
"""
