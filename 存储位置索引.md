# 存储位置索引

这份文档记录**什么放在哪**。改动存储位置时请一并更新它。

最后更新：2026-09-19

---

## 一句话

**代码在 E 盘，数据在 F 盘。**

| | 位置 | 说明 |
|---|---|---|
| 代码仓库 | `E:\text_to_cad_improve\auto_detection_process\` | 源码、测试、管线 |
| 运行产物 | `F:\text_to_cad_improve\auto_detection_process\_loop_test\` | 闭环每一轮的完整证据（1962 文件 / 32 GB） |
| 数据集生成物 | `F:\text_to_cad_improve\auto_detection_process\_param_experiment\output\` | D01–D32 家族的模型与数据集（6378 文件 / 16 GB） |

F 盘布局**镜像** E 盘的仓库路径，所以找法是机械的：把开头的
`E:\text_to_cad_improve\auto_detection_process\` 换成
`F:\text_to_cad_improve\auto_detection_process\` 即可。

---

## 两个盘

| 盘 | 类型 | 文件系统 | 容量 | 空闲 |
|---|---|---|---|---|
| E: | USB **SSD**（快） | **exFAT** | 932 GB | 见 `df` |
| F: | USB **机械盘**（慢） | NTFS | 1.9 TB | 见 `df` |

两件事值得记住：

1. **E: 是 exFAT，所以不能建 junction 或符号链接。** 想"数据在 F、路径还在 E"这种透明重定向在这台机器上做不到。代码里因此用了**显式回退**（见下）。
2. **F: 是机械盘。** 网格和求解的 I/O 走它比走 E: 慢。大文件搬运和求解读盘会感觉得到。

---

## 代码怎么找到这些目录

因为 exFAT 不能建链接，位置是通过**两个小模块**解析的，而不是写死的路径：

| 模块 | 提供 | 解析到 |
|---|---|---|
| `_structural_experiment/_store.py` | `loop_test()` | `_loop_test` 所在盘 |
| `_param_experiment/_paths.py` | `output_root()` / `under(*parts)` | `output` 所在盘 |

两个都**同时支持搬迁前和搬迁后的布局**：先看搬迁后的位置（且要求**非空**——
目录从创建起就存在，搬完前一直是空的，只看"存在"会把脚本指向一棵空树并
表现得像"数据没了"），否则回退到原来旁边的位置。所以：

- 从没搬过的检出和搬过的检出行为一致；
- F 盘临时离线时，脚本会用本地的那份而不是直接失败。

`_param_experiment/*.py` 里原有的 39 处 `_HERE / "output"` 已全部改为
`output_root()`（29 个文件）。另有 4 个用硬编码绝对路径的脚本一并改了。

---

## 为什么 `_param_experiment` 的**代码**没搬

仓库根目录的定位**依赖这些目录的存在**，而且三处探测用的标记不一致：

| 位置 | 找什么标记 |
|---|---|
| `tools/parametric.py:find_repo_root()` | `_param_experiment` |
| `core/mesh_feedback.py:_find_repo_root()` | `_structural_experiment` **且** `integrations/engineering_tools/src` |
| `cli.py`（面映射那段） | `_structural_experiment` |

所以 `_param_experiment` 和 `_structural_experiment` **都不能整个搬走**——搬走
任何一个都会让某条路径找不到仓库根。**数据可以搬**（`output` 和 `_loop_test`
已经搬到 F 盘），**目录本身不行**。

同理，这些也留在 E 盘（都小，且被代码按固定路径读取）：

- `_param_experiment/turbine_disc_dataset_D01-D32/scripts/` —— **母版模板**（1.1 GB），
  被 `run_loop.py` 的 `MASTER` 常量和 `workspace.verify_master_untouched()` 引用
- `_param_experiment/param_templates.py` —— 在 `sys.path` 上被 import
- `_structural_experiment/input/` —— 运行起始用的 brief

---

## 运行产物怎么看

一次闭环运行（例如 `run6`）的结构：

```
<运行目录>\run6\
  loop_result.json          ← 权威结果：每轮的峰值、观测、判定、噪声底
  knowledge.json            ← 知识库（种子规则 + 本轮学到的）
  revisions\rev-000001\
    document.base.json      ← 这一轮拿到的设计
    document.json           ← 这一轮改完的设计（两者相减 = 这一轮改了什么）
    output\generation.json  ← built_from：从哪来的
  jobs\jobs\D27-rev-000001\
    case.json               ← 这一轮的实验条件（domain / 选面 / 网格方案）
    agent\*.json            ← 各 agent 的决策与 tool trace
    solve\structural_metrics.json   ← 峰值、安全系数、载荷审计
    mesh\convergence\       ← 两级网格研究 → 噪声底的数据来源
```

**最有用的三个文件**，按顺序：

1. `<运行目录>\loop_result.json` —— 结论在这里
2. `<运行目录>\knowledge.json` —— 学到了什么
3. `<运行目录>\jobs\jobs\<job>\agent\domain.json` 等 —— 想追问"为什么这么决定"时看

各轮次做了什么、结论如何，见 `auto_detection_process` 里的会话记录与
`_loop_test/run*/` 下的日志（`run3.log` … `run6.log`）。

---

## 改存储位置怎么改

1. 搬目录（用 `robocopy /MOVE`，别用资源管理器剪切——32 GB 中途失败会两边都不完整）
2. 改这两个模块里的 `_MOVED` 常量：
   - `_structural_experiment/_store.py`
   - `_param_experiment/_paths.py`
3. 更新本文档

不需要改别的地方——所有引用都走这两个模块。
