"""Thinking Stress Benchmark v1 — Code repair closed-loop benchmark.

Proves DeepSeek thinking mode value through engineering execution tasks:
read broken code → run tests → locate root cause → patch → regress → hidden tests.
"""
